# MDL 项目维护与协作手册

> **本文件是 MDL 项目唯一的协作约定文档**（agent 与同事共用）。
> 所有 agent 在 `mdl/` 目录工作时，先读本文件。
>
> 维护规则：
> - 改设计 → 更新对应章节，并同步 bump 章节版本标记；
> - 完成工作 → 在 **§9 工作日志** 追加条目（新条目在最上面）；
> - 认领任务 → 在 **§8 任务看板** 写上你的名字/agent id 和状态；
> - §10 是同事专属工作区，自由使用，他人勿动。

---

## 目录

1. [一句话目标](#1-一句话目标)
2. [核心设计决策](#2-核心设计决策已定勿违背)
3. [当前状态](#3-当前状态)
4. [MCP 接口定义](#4-mcp-接口定义草案-v01)
5. [编译期资源隔离](#5-编译期资源隔离规格)
6. [目录地图](#6-目录地图)
7. [关键契约文件](#7-关键契约文件)
8. [任务看板](#8-任务看板)
9. [工作日志](#9-工作日志)
10. [同事工作区](#10-同事工作区)
11. [LuatOS 差距复核 + ESP32/RISC-V 可移植性](#11-luatos-差距复核--esp32risc-v-可移植性2026-09-07-主线-agent)

---

## 1. 一句话目标

让 AI agent 通过 **MCP（Model Context Protocol）** 对 AT32F435（Cortex-M4）设备
**无烧录、无整机复位**地完成设备功能的替换与移除。

模块代码在 PC 侧编译为 PIC 的 `.mdl` 包，经 USB CDC 下发，
以非特权 FreeRTOS-MPU 任务运行在 SRAM arena 中。

---

## 2. 核心设计决策（已定，勿违背）

### D1 — 单槽互斥（v1 铁律）

- 设备同一时刻**最多一个模块**在跑，`g_mdl_slot` 唯一。
- "换功能" = UNLOAD（删任务、整池回收内存）→ LOAD（搬代码、建任务）。
  旧模块任务必须已删除、槽位回到 `EMPTY` 后新模块才能加载。
- **运行时永远不存在两个模块并存，因此不存在运行时资源竞争。**
- 固件侧 LOAD 遇槽忙必须拒绝（`mdl/core/supervisor.c:46` 现状即如此，保持）。
- 多槽位是 v2 的事；若要做，须先回本节改写本决策并讨论资源仲裁。

### D2 — 资源竞争在编译期隔离和明确

- 资源（GPIO 引脚等）的合法性检查**全部前移到 PC 侧编译/pack 阶段**，
  设备运行时只做兜底校验（SVC handler 白名单检查保持现状）。
- 模块源码用宏声明资源（见 §5.2），编译进只读 manifest 符号；
  `tools/packer.py` 从 ELF 读出并与固件白名单比对——不合法**拒绝打包**，
  根本到不了设备。语义化资源冲突同样在 pack 期发现。
- 原则：**能静态确定的，绝不留到运行时**；运行时校验只是最后防线。

### D3 — 版本化 ABI，append-only

`host_api_t` 只增不改、版本号 bump；packer 在 pack 期比对模块与固件的
`HOST_API_ABI_VERSION`，不匹配拒绝。

---

## 3. 当前状态

| 里程碑 | 内容 | 状态 |
|:---:|---|---|
| M0 | MPU 区域编程自检 | 代码完成，未上真机 |
| M1 | LOAD + GOT 重定位 | 代码完成；**QEMU 已验证** |
| M2 | 非特权任务运行 | 代码完成，未上真机 |
| M3 | 故障恢复 + 软件看门狗 | 代码完成，未上真机 |
| M4 | USB CDC 传输 + 文本控制台 | **真机跑通**（枚举 COM5，控制台交互正常，288MHz 已实测） |
| MCP-A | MCP server（PC 侧） | **未开始 — 下一里程碑** |
| MCP-B | 资源 manifest + packer 编译期检查 | **未开始 — 随 MCP-A 一起做** |

**硬件已到位（2026-09-05）**：UYUP-RPI-A-2.4 底板（STM32F103VCT6 兼容 LQFP100），
实焊 **AT32F435VCT7**（256K flash / 384K SRAM / **24MHz 晶振**），外部 J-Link 接 H2。
板级事实、测试步骤、判定标准全部在 `mdl/tests/hil/ozone/README.md`，那里是唯一参考。

三个必须知道的板级前提：
1. **`DAP_CONF` 必须先短接到 GND 再上电**，否则板载 STM32F042 CMSIS-DAP 会和
   外部 J-Link 抢 SWDIO/SWCLK；
2. Ozone 只支持 SEGGER 探针，**用不了 CMSIS-DAP**，这条路不通；
3. **USB1 必须用 A-to-C 线**（2026-09-06 实测）。这块板的 USB-C 座作为 device
   缺 CC 上的 5.1k 下拉（Rd），Type-C 主机因此判定"没插设备"，
   **C-to-C 线下主机连"未知设备"都不显示**，任何固件都救不了。
   A 口没有 CC 协商，不受影响。排查 USB 先换线再怀疑代码。

原"板级配置不符"问题（芯片料号 define、Flash 长度、HEXT 晶振值）已于 2026-09-06 全部修正。

**M4 首跑 BusFault：已定位、修复并在真机验证（2026-09-06）。**

根因是链接脚本 `at32f435_m2/linker/AT32F435xC_MDL_MPU.ld` 的一个覆盖空洞：

```
.privileged_data   0x20000000..0x20008000  32KB   ← 既不拷贝也不清零
_sdata.._edata     0x20008000..0x20008100         ← 拷贝只覆盖 .data
_sbss.._ebss       0x20008100..0x20011020         ← 清零只覆盖 .bss
```

厂商启动代码只做两件事：`_sidata → [_sdata,_edata)` 拷贝、`[_sbss,_ebss)` 清零。
而 `_sidata` 原本写成 `LOADADDR(.data)`、`_sdata` 落在 `.data` 开头，
于是夹在中间的 `.privileged_data`（是**有 flash 初值的 loaded 段**）两头不靠。
FreeRTOS 全部 `PRIVILEGED_DATA` 对象——`pxCurrentTCB`、`pxReadyTasksLists[]`、
`uxCurrentNumberOfTasks`、`uxCriticalNesting`（初值 `0xaaaaaaaa`，不是 0）、
heap_4 的 `ucHeap`——上电即为 SRAM 随机值。

真机表现：`pxCurrentTCB` 垃圾非 NULL → `prvInitialiseTaskLists()` 从不执行 →
`pxReadyTasksLists[]` 是垃圾 → 首个 `xTaskCreate()` 在 `vListInsertEnd` 的
`str r1,[r2,#4]` 上写向非法地址 → 精确 BusFault（`CFSR=0x00008200`）。

**同一个根因极可能也是 2026-09-04 起悬而未决的 QEMU PendSV/MemManage 阻塞**：
QEMU 的 RAM 上电全零，`pxCurrentTCB==NULL` 侥幸正确，但 `uxCriticalNesting`
变成 0 而不是 `0xaaaaaaaa`，临界区嵌套计数从一开始就错——同因不同症。
修好后应重跑 QEMU 目标验证这个推断。

修法：`_sidata = LOADADDR(.privileged_data)`，`_sdata` 移到 `.privileged_data`
开头，`_edata` 仍在 `.data` 结尾；两段都是相邻的 `AT> FLASH`，LMA 连续
（已核对 `0x08016b2c + 0x8000 == 0x0801eb2c`），一次 memcpy 覆盖两段。
**不要把 `_sdata` 挪回 `.data`，也不要在这两段之间插入新的 loaded 段。**

排查方法本身值得记住：首次报的是 **IMPRECISERR**（`CFSR=0x00000400`，
`BFARVALID=0`），此时 PC 与真正出错的指令无关，顺着它查是查噪声。
`mdl/tests/hil/common/board_debug.c` 置 `ACTLR.DISDEFWBUF` 关掉写缓冲后
才变成 PRECISERR，一步定位。上真机排 bus fault 先开这个。

---

**M4 第二个真机 bug：宿主任务漏了 `portPRIVILEGE_BIT`（已修并验证）**

BusFault 修好后调度器起来了，随即在任务切换处停在 MemManage，
`CFSR = 0x00000008` = **MUNSTKERR**（异常返回出栈被 MPU 拒绝）。

根因：**全仓库从来没有用过 `portPRIVILEGE_BIT`**。FreeRTOS-MPU 里
`xTaskCreate()` 默认产生**非特权**任务，要特权必须显式
`uxPriority | portPRIVILEGE_BIT`。而 `supervisor_task` / `usb_task` /
`loader_task` / `watcher_task` 都是宿主基础设施：要写 `g_mdl_slot`（host .bss）、
要配 GPIO/CRM/OTG 寄存器、要驱动 USB 协议栈，全部需要特权。

而且它根本活不到访问那些资源：`xTaskCreate` 的任务栈从 FreeRTOS 堆分配，
heap_4 的 `ucHeap` 是 `PRIVILEGED_DATA`，`prvSetupMPU()` 把整个
`privileged_data` 段标为仅特权可访问 —— 非特权任务的 PSP 指进它够不着的内存，
**第一次异常返回到该任务时就 MUNSTKERR**。

已给 M2/M3/M4 及 QEMU 目标的宿主任务全部加上该位；
**唯一不能加的是模块任务**（`mdl/core/module_task.c`，非特权是它的全部意义，
且它的栈来自 arena 不是堆）。约定写在 `at32f435_m2/inc/FreeRTOSConfig.h`
`MDL_LOADER_TASK_PRIORITY` 旁边。

**这同时是 QEMU 那个"第二次任务切换 MemManage"阻塞的头号嫌疑**：
同样的 CFSR 值、同样的机制。QEMU 目标已一并加上该位，待复跑确认（H10）。
（`.privileged_data` 未初始化那条之前也被列为 QEMU 嫌疑——两条都真实存在，
QEMU 侧到底是哪一条、或两条都有，要跑过才知道。）

**构建纪律（2026-09-06 教训，重要）**：
"五个里程碑都编译通过"此前只在**各自落地当时**成立。首次五个一起编，发现
**M0/M1/M3 已坏了很久**——每个后续里程碑往*共享*文件加了早期裸机目标满足不了的依赖
（M3 把 `supervisor.h` 塞进 `fault_arm.c` 坏了 M0/M1；M2 把 `host/host_api.c` 改成依赖
FreeRTOS 坏了 M1；M4 把 `protocol.h` 塞进 `supervisor.c` 坏了 M3），而每次只重编了最新那个。
**没有 CI，所以提交前必须 `mdl/tests/hil/build.ps1 all -Clean` 五个全过。**

- 工具链：arm-none-eabi-gcc 12.2、Python 3.12；本机 QEMU 未装。
  `make` 是 **`mingw32-make.exe`**（WinLibs mingw64），且需要 Git 的 `usr/bin`
  提供 `mkdir -p` / `rm -rf`。别手拼，用 `mdl/tests/hil/build.ps1`。
- vendor 库路径为 `at32f435_lib/AT32F435_437_Firmware_Library_V2.2.6/`。

**已知技术债**（按优先级）：

1. 看门狗喂狗信号 = "调用了 host API"，CPU 密集模块超 3s 会被误杀；
2. ~~MDL 自身不显式置 `SCB->SHCSR.MEMFAULTENA`~~ → **已修**（`mpu_armv7m.c`
   `arch_setup_regions()` 末尾，与 MPU 使能同处）；
3. `.init_array` / C++ 静态构造不运行；
4. alloc 池双 free 不检出；
5. 无鉴权：能打开 CDC 端口即可 LOAD 代码；
6. 无 CI；测试为分散的 HIL/QEMU/mock 三套。
7. ~~M4 的 `main()` 从不调用 `sandbox_init()`，arena 边界为 0~~ → **已修**：
   拆出 `sandbox_bounds_init()`（只记边界、不碰 MPU），M3/M4 都调用它。
   真机 `arena` 已读回 text 0x20020000 / data 0x20024000 / heap 0x20026000 /
   guard 0x20028000，与链接脚本布局一致。

---

## 4. MCP 接口定义（草案 v0.1，冻结后实现）

- 形态：`mdl/tools/mcp_server/` 下的 Python MCP server，**stdio 传输**；
  通过 pyserial 操作 CDC 端口，server 内维护单连接，并发调用串行化。
- 设备返回的 UTF-8 错误串一律原样带回 tool result，不吞错。

### Tools

| Tool | 参数 | 返回 | 语义 |
|---|---|---|---|
| `describe_device` | — | `{abi_ver, arch, slot_state, gpio_whitelist, arena:{text,data,heap_stack}, watchdog_ms, transport}` | 设备能力发现 |
| `compile_check` | `source: string` | `{ok, diagnostics[], manifest{resources, sizes}}` | **只检不烧**：mock_host ASan 预检 → arm 交叉编译 → pack（含 manifest 校验）。agent 改代码的 inner loop 用它 |
| `deploy_feature` | `source: string`, `name: string` | `{ok, device_response, unloaded_previous: bool}` | 完整链：compile_check → 槽忙则 UNLOAD（原子替换）→ LOAD → 回读设备 OK/ERROR |
| `remove_feature` | — | `{ok, device_response}` | UNLOAD；槽已空返回 ok（幂等） |
| `device_status` | — | `{slot_state, fault_pc?, fault_text_offset?, uptime_ms}` | 槽位/故障查询；`fault_text_offset` 供 addr2line 定位模块内崩溃 |
| `read_logs` | `max_lines: int = 50` | `{lines[]}` | tail 设备日志（USB CDC） |

### 错误契约

- 所有 tool 返回 `{ok: bool, error?: string}`；`error` 直接面向 LLM，
  写明原因 + 下一步建议（沿用 `tools/packer.py` 现有错误文案风格）。
- `deploy_feature` 在 compile_check 失败时**不触碰设备**。
- 传输超时/断连：error 中报告端口状态，不自动重试（留给 agent 决策）。

---

## 5. 编译期资源隔离（规格）

### 5.1 规则

1. 模块资源声明齐全才可打包；声明与实际不符 = bug，pack 期能抓多少抓多少。
2. 声明的每个资源必须属于目标固件的 compile-time 白名单，否则拒绝。
3. v1 单槽下"两模块争同一资源"的语义 = **替换**：`deploy_feature` 返回
   `unloaded_previous: true`，agent 在 compile_check 输出里能看到旧 manifest。
4. 运行时 SVC 白名单校验保持现状，仅作兜底。

### 5.2 机制

模块文件作用域声明（与 `MDL_MODULE_ABI_DECLARE()` 同风格）：

```c
MDL_MODULE_ABI_DECLARE();
MDL_MODULE_RESOURCES(GPIO(13), GPIO(14));  /* 宏生成只读 const 段 */
```

- packer 从 ELF 提取 `__mdl_resources` 符号 → 逐项对白名单 → 写进
  `.mdl` header 预留字段或 sidecar JSON，供 `compile_check` 与 STATUS 上报。
- 未来多槽位时，同一套 manifest 就是资源仲裁的输入，机制不变。

---

## 6. 目录地图

```
mdl/
├── tools/
│   ├── packer.py        ELF32 解析 + 校验 + 打包（MCP-B 的落点）
│   ├── watch.py         人工 inner loop（MCP server 将取代其角色）
│   ├── mock_host/       原生编译 + ASan 预检
│   └── mcp_server/      【待建】MCP server
├── transport/           USB CDC 厂商驱动 + MDLC 帧协议
├── core/                loader / supervisor / module_task / sandbox / registry
├── arch/arm_cm4/        MPU、故障恢复、GOT 重定位、r9 PIC 切换
├── host/                host_api_t vtable + SVC 门（唯一模块→宿主通道）
├── linker/              mdl_arena.ld（64K SRAM arena）
└── tests/               HIL m0–m4 / QEMU cmsdk_m4 / 7 个故障注入模块
```

## 7. 关键契约文件

| 文件 | 契约 |
|---|---|
| `mdl/core/mdl_format.h` | .mdl 线格式、重定位类型 |
| `mdl/host/host_api.h` | 模块 ABI（vtable + SVC 语义） |
| `mdl/transport/protocol.h` | MDLC 帧协议 |
| `mdl/linker/mdl_arena.ld` | 内存布局（arena 划分） |

---

## 8. 任务看板

### 8.1 优先级路线图（2026-09-07 定）

排序依据只有一条：**先补漏，再补课，最后才是拉开差距。**
把差异化功能建在一个正在漏的地基上，是这个项目最容易犯的错。

---

**P0 — 今天就在漏的洞。做完之前，别的都先放着。**

| # | 任务 | 量 | 为什么是 P0 |
|:---:|---|---|---|
| R1 | 卸载时把引脚恢复到安全态 | 半天 | `reclaim_module()` 现在**完全不碰 GPIO 配置**。MDL 把引脚配成什么样，卸载后就留成什么样，下一个 MDL 继承到一个状态未知的引脚。现在被掩盖着（MDL 只用 GPIO，而两个指示灯 host 会主动收回），换成 I2C/ADC 立刻暴露。**多外设仲裁的全部工作都压在这块地基上。** |
| R2 | 关掉 `BOARD_DEBUG_PRECISE_BUS_FAULTS`，或改成构建开关 | 一行 | 它设 `ACTLR.DISDEFWBUF`，**全系统每条 store 都变慢**。这是 bring-up 工具（H8/H12 都靠它定位），但留着它测出来的性能数字全是虚低的。**"几乎无性能损耗"是本项目的核心主张，拿错的数去对标 LuaTOS 会当场翻车。** |

---

**P1 — 没有这些就没资格谈对标 LuaTOS。**

| # | 任务 | 量 | 说明 |
|:---:|---|---|---|
| H2 | `watchdog_feed` 进 ABI，改掉"隐式喂狗" | 小 | **这是 F1 的前置，容易被漏。** 现在的喂狗信号是"调过任何 host API"，`sw3_blue` 靠 20ms 轮询恰好满足。F1 一落地，常驻 MDL 会**阻塞等事件**——几秒不调 API 是正常状态，会被当场误杀。先改语义，再做 F1。 |
| F1 | 中断 / 回调（引脚 EXTI、外设、定时器） | 大 | LuaTOS 有，我们没有——**纯补课项，不是差异化项**。架构和它一样是两段式（host ISR 记录 → 唤醒模块任务 → 非特权 handler），但我们的后半段是原生代码，没有解释器和 GC 停顿。要先定死一件事：**事件产生快于消费时的策略**（丢最旧 / 合并 / 只计数）。不明确定下来，将来必然变成难查的丢事件问题。 |
| R3 | 资源类型扩展：TIMER / I2C / UART / SPI / ADC，并按**引脚**判冲突 | 中 | 中断源本身就是资源（EXTI 线、定时器通道），F1 落地时必然要它。关键设计点：`MDL_RES_I2C(1)` 必须在 host 侧**展开成物理引脚**再比对——两个名字不同的声明可以撞在同一根线上（"I2C1_SDA" vs "GPIO 5"），只比名字必漏判。需要一张板级"功能↔引脚"映射表。 |

---

**P2 — 把已有的领先变成可证明、可自动化的护城河。**

| # | 任务 | 说明 |
|:---:|---|---|
| B2 | packer 提取 manifest + 与目标白名单比对 | 兑现 D2。**这才是"强制修改"的正确位置：让构建失败，而不是让加载失败。** 加载期拒绝是最后一道防线，不是第一道。 |
| P5 | 基准测试（对 LuaTOS / MicroPython） | 依赖 R2。要测的是**调度后 handler 的执行时间**和**事件到 handler 的延迟**，不是空转跑分。 |
| A1–A3 | MCP server | 让 agent 能直接编译/部署/查状态。 |

---

**P3 — 重要但不紧急。**

| # | 任务 | 说明 |
|:---:|---|---|
| F2 | flash 持久化 | MDL 现在在 SRAM，掉电即失。演示够用，产品不够。 |
| F3 | 原子热替换 | 现在是 unload → load 两步，中间有空窗。 |
| H10 | 复跑 QEMU 目标 | H12 大概率就是它的根因，跑一遍即可销账。本机未装 QEMU。 |

---

### 8.2 对标 LuaTOS 的定位（2026-09-07）

| 维度 | LuaTOS | 我们 | |
|---|---|---|---|
| 中断/回调 API | 有 | **无** | 落后，P1 补 |
| 回调执行 | 解释 + GC 停顿 | 原生，无 GC | 领先一个量级 |
| 用户逻辑定时精度 | ms 带抖动 | μs + 一次调度 | 领先 |
| 资源冲突检测 | 基本没有（后设置的静默覆盖） | 声明式 + 三道拦截 | **结构性领先** |
| 硬件隔离 | VM 内存安全（挡不住合法 API 改引脚复用） | MPU，且**无外设区域** | 领先 |
| 热替换 | 通常需重启 | 不重启不复位 | 领先 |
| **驱动/生态广度** | 数百个现成库 | 一个 GPIO | **差距巨大** |

> 结论要拆成两句话：**架构和确定性上超越它**——补完 F1 基本成立；
> **生态广度上超越它**——短期不可能，也不该作为目标。
>
> 真正该盯的是那些 LuaTOS **因为架构而做不到**的场景：μs 级确定性、
> 多方共用外设不静默打架、现场不断电改功能。

---

### 8.3 任务表

> 认领方式：在"负责"列写上名字/agent id，状态改为进行中。
> 约定状态：`待认领` → `进行中` → `已验证` → `完成`

| # | 任务 | 负责 | 状态 | 备注 |
|:---:|---|---|---|---|
| A1 | MCP server 骨架 + stdio 收发 | claude/opus-5 | **已验证** | 手写 JSON-RPC，不引入 mcp SDK；真机端到端通过 |
| A2 | `compile_check` 工具（接 mock_host + packer） | claude/opus-5 | **已验证** | 离线默认；`against_device` 可选在线核对 ABI/槽位 |
| A3 | `deploy/remove/status/logs/describe` 工具 | claude/opus-5 | **已验证** | 真机 deploy/unload/logs 全通；构建失败不碰设备已验证 |
| B1 | `MDL_MODULE_RESOURCES` 宏 + `__mdl_resources` 段 | claude/opus-5 | **已验证** | ABI v2；真机拒绝 PA9 认领，报错点名持有者 |
| B2 | P2 packer manifest 提取 + 引脚级冲突比对 | claude/opus-5 | **已验证** | 兑现 D2；"强制修改"的正确位置是构建期而非加载期 |
| R1 | **P0 卸载时恢复引脚到安全态** | claude/opus-5 | **已验证** | `reclaim_module()` 不碰 GPIO 配置，残留会传给下一个 MDL |
| R2 | **P0 关掉 DISDEFWBUF（写缓冲）** | claude/opus-5 | **已验证** | 一行；不改则所有性能数字虚低 |
| R3 | P1 资源类型扩展 + 引脚级冲突展开 | claude/opus-5 | **机制已验证** | TIMER/I2C/UART/SPI/ADC；需板级功能↔引脚表 |
| F1 | P1 中断 / 回调（两段式派发） | claude/opus-5 | **已验证** | ABI v3；先定事件队列溢出策略；依赖 H2 |
| F2 | P3 flash 持久化 | claude/opus-5 | **已验证** | flash 末 16K；真机断电重插后自动恢复 |
| F3 | P3 原子热替换 | claude/opus-5 | **已验证** | 先校验后拆除；坏镜像被拒时旧 MDL 仍在跑 |
| P5 | P2 基准测试（对标 LuaTOS） | claude/opus-5 | **已验证** | 三个数全部真机取得，308 样本；见 §9 |
| H1 | 真机 HIL 配置修正（ozone README 板级问题） | claude/opus-5 | **完成** | 料号/Flash 长度/HEXT 全改；五目标重编通过 |
| H2 | **P1 看门狗误杀 → `watchdog_feed` 进 ABI** | claude/opus-5 | **已验证** | **F1 的前置**：常驻 MDL 阻塞等事件时会被隐式喂狗机制误杀 |
| H3 | 技术债 #2：显式置 MEMFAULTENA | claude/opus-5 | **完成** | `mpu_armv7m.c` `arch_setup_regions()` |
| H4 | 修复 M0/M1/M3 构建腐坏 + 加 `build.ps1` | claude/opus-5 | **完成** | 见 §3 构建纪律 |
| H5 | 24MHz PLL（`common/board_clock.c`）+ USB 48MHz | claude/opus-5 | **已验证** | 真机 `clk` 读回 288000000 |
| H6 | 非特权 trampoline 访问 host RAM（必炸） | claude/opus-5 | **完成** | 走 `freertos_system_calls` 提权；要等真的加载模块才算验证 |
| H7 | USB CDC 文本控制台（与 MDLC 帧共用一个口） | claude/opus-5 | **已验证** | 真机 COM5 交互正常；MDLC 帧共用尚未实测 |
| H8 | **M4 真机 BusFault 定位** | claude/opus-5 | **已验证** | 根因：链接脚本 `.privileged_data` 未被启动代码初始化，见 §3 |
| H10 | 复跑 QEMU 目标，验证 PendSV 阻塞是否同因 | claude/opus-5 | **部分完成** | bug 部分已由 AT32 间接验证；目标已修复腐坏并纳入 build.ps1；真跑仍需装 QEMU |
| H11 | **宿主任务漏 `portPRIVILEGE_BIT` → MUNSTKERR** | claude/opus-5 | **已验证** | M2/M3/M4/QEMU 全部补上；模块任务保持非特权 |
| H12 | **模块加载必炸设备（8B 空模块也炸）** | claude/opus-5 | **已验证** | 根因：`module_task.c` 把 `tskMPU_REGION_*` 抽象标志当 RASR 位传给 ARMv7-M 端口 → AP=000 + SIZE 被撑大；见 §9 |
| H13 | fault 记录跨复位存活 + `fault` 控制台命令 | claude/opus-5 | **已验证** | `.noinit` 段；fault 后主动复位而非锁死。H12 和 H15 两个根因都是靠它读出来的 |
| H15 | **`host->log()` 用半主机，无调试器时必炸** | claude/opus-5 | **已验证** | `bkpt 0xAB` 在 C_DEBUGEN=0 时升级为 HardFault(DEBUGEVT)；改为可替换 sink，M4 走 USB 控制台 |
| H14 | `build.ps1` 把编译失败报成 BUILD OK | claude/opus-5 | **完成** | 旧逻辑靠 `Test-Path firmware.elf`，陈旧 elf 即绿灯；改用 make 退出码 + error/warning 分离 |
| H9 | 技术债 #7：M4 arena 边界为 0 | claude/opus-5 | **已验证** | 拆出 `sandbox_bounds_init()`；M3 内联版还漏了 guard 边界 |
| X1 | RV32 PIC 可行性 POC | | 待认领 | `gp` 基址 + `-mno-relax` + packer 加 `R_RISCV_RELATIVE`；裸 RV32 目标，不碰 ESP-IDF。见 §11.2 |
| X2 | ESP32-C3 集成评估 | | 待认领 | 依赖 X1；主成本是 ESP-IDF FreeRTOS 分支与上游 MPU 端口合并 |

---

## 8.5 2.0 计划：从「能换一个功能」到「能跑一个系统」

> 排定于 2026-09-13。触发点是对比表里暴露的两项差距：**同时驻留**（我们一次一个）
> 与**即写即跑**（我们要过工具链）。下面先给出这两项的分析，再排任务。

### 分析一：竞品的「同时驻留」到底是什么

**它们没有「槽位」这个概念。** MicroPython 和 LuaTOS 的模块就是一个堆里的对象，
在同一个 VM、同一个地址空间、同一个特权级上。数量只受 RAM 限制，几十个是常态。

> **它们的无限并存，恰恰来自没有隔离。** 一个堆、一个 GC、一个特权级——模块 A 的
> bug 直接破坏模块 B 的数据。这是它们做的交换。

所以「对标同时驻留」不是去追它们的数字，而是回答：**我们能负担几个隔离槽位，
这个数够不够用。**

**约束核实（不是推测）：**

| 约束 | 实际情况 |
|---|---|
| MPU 区域 | `configTOTAL_MPU_REGIONS = 8`，但**区域是每任务的、上下文切换时重编程**。所以 MPU **不限制槽位数量**，只限制每槽位的区域数（目前用 2 个） |
| RAM | 每 MDL = text + data + heap 4K + guard 2K + stack 2K。384K SRAM 装得下 |
| **真正的约束** | ARMv7-M 要求区域**2 的幂且自然对齐**。链接脚本现在用 `ALIGN(32768)` / `ALIGN(8192)`。多槽位需要一个满足对齐的分配器 |

**结论：多槽位可行，而且槽位数不该写死。**

> 固定 N 个槽位是偷懒。**每个 MDL 的大小由它的业务复杂度决定**，固定划分要么浪费
> 要么不够。正确的形态是**按需从 arena 分配**，而两端的先验**已经各有一半**：

```
packer 侧:  text=1040B data=0B bss=112B     ← 知道这个 MDL 要多少
设备侧:     arena 命令报告 text/data/heap 边界  ← 知道自己有多少
```

缺的是**协商**：上位工具应该在推送**之前**就能回答「装不装得下现在这些」。
`compile_check` 的 `against_device` 已经是这个形状。

**但对齐会产生碎片，这一条要如实说。** ARMv7-M 要求区域 2 的幂且自然对齐，
所以卸载一个 3K 的、想装一个 5K 的，可能总空闲够而对齐后装不下。

> **整理碎片对我们特别难**：加载时我们确实做重定位，但一个**跑起来**的 MDL 的
> 栈和堆里有**指向它自己的指针**，那些我们无从改写。

**追踪那些指针会把泄漏风险引进来，而这个风险可以整个绕开。**

要整理碎片就得追踪栈堆里的指针，而追踪器**要么漏指针（崩溃）、要么过度保留（泄漏）**
——那正是 GC 的工作，而我们刚刚拒绝了 GC。

但那个风险**来自细粒度分配，而我们没有细粒度分配**：

> **一个 MDL 就是一整块：整块分配、整块释放。它自己的堆和栈都在这块里面**，
> 块一释放全部随之消失。
>
> 块外指向块内的引用只有宿主侧那几个，**全部可枚举且已在 `reclaim_module()` 里
> 逐个清除**：`task_handle`（vTaskDelete）、`entry`/`cmd_entry`/`evt_entry`、
> 事件路由的 `s_module_task`（disarm_all）、api pool。
>
> **所以不存在需要追踪的内部指针，也就不存在泄漏风险的来源。**

**而 MPU 的对齐要求反过来给了现成解：buddy 分配器。**

2 的幂对齐看着是负担，却正好是 buddy 的前提条件，而它的性质对上了这个问题：

| buddy 的性质 | 对我们意味着 |
|---|---|
| **释放时自动合并** | 卸载一个 MDL，空间真的变回可用的大块，而不是一堆碎渣 |
| 内部碎片有界（≤50%） | 5K 的 MDL 占 8K 块——**这是 MPU 对齐的价格，不是 bug** |
| 不需要追踪指针 | 因为粒度就是「一个 MDL 一块」 |

**唯一诚实的代价是内部碎片**，而它可预测、可报告：上位工具能提前说「这个 MDL 需要
8K 对齐块，当前最大连续块 4K，建议先卸载 X」。**一个说得清为什么装不下的分配器，
比一个偶尔神秘失败的好得多。**

### 分析二：即写即跑，与为什么 2.0 不做 REPL

**REPL = Read-Eval-Print Loop** —— 串口上一个 `>>>` 提示符，敲一行立刻执行并打印。

它要求**执行没有预先编译过的代码**，而这对本项目有根本性张力：

| 路径 | 代价 |
|---|---|
| 内置解释器 | **放弃原生速度**——那是整个项目的立论 |
| 主机逐行编译再推送 | 就是现状 |
| 控制台扩成硬件 shell | 不需要解释器，覆盖 REPL 的主要用途 |

**现状实测（2026-09-13，i2c_scan 1024 B）：**

```
编译+链接+打包   218 ms
推送+加载+运行   498 ms
一次完整往返     715 ms
```

**不到一秒。** 差距不在速度，在于**需要人手动触发**，以及**设备上无法探索**。

> 嵌入式场景里用 REPL 干的事九成是**戳硬件**：读寄存器、拉高一个脚、试一次传输。
> 那不需要解释器，只需要控制台命令——我们已经有一半（`led`/`btn`/`mem`/`adc`/`spi`/`pins`）。

**还有第四个选项，它把这个取舍整个化解掉：把解释器做成一个可选的 MDL。**

想要 REPL 的人加载一个 Lua 解释器 MDL。它在沙箱里跑，和别的 MDL 一样受 MPU 约束、
一样占一份 arena。**不加载时成本为零。** 需要速度的模块继续是原生的。

> 于是这不再是「全局二选一」，**变成逐模块选择**——而这只有在有槽位机制的前提下
> 才成立。**它是多槽位的红利，不是另一个项目。**

REPL 真正提供的是三样东西，现在三样都有答案：

| REPL 的价值 | 我们的答案 |
|---|---|
| 缩短「试一下」的循环 | W1 watch 模式，把 715 ms 变成无感 |
| 在设备上戳硬件 | W2 硬件 shell，**不需要解释器** |
| 表达式之间保持状态、对活着的数据求值 | 原生做不到——**解释器 MDL 能**（S8） |

### 分析三：能不能兼容 LuaTOS / MicroPython 的库

**字面意义上做不到。** 它们的库是**给一个我们没有的解释器用的源码**——要运行它们
就得内置解释器（或者按上面说的，把解释器做成一个 MDL）。

**但那些库里最值钱的东西不是 Python/Lua 本身：**

| 层 | 能不能拿 |
|---|---|
| `.py` / `.lua` 源码 | **拿不了**——需要解释器 |
| 底下那层 **C 驱动** | **能拿**，两者的驱动本来就是 C |
| **器件知识**：寄存器映射、初始化时序、勘误规避 | **能拿，而且这是最值钱的部分**，与语言无关 |

所以策略是 **API 形状兼容，不是源码兼容**：把 vtable 命名对齐到 `machine.I2C` /
`machine.Pin` 那套，让移植一个 MicroPython 驱动变成**近乎逐行的机械翻译**。

> 这条和下面的 agent 优先直接咬合：**移植由 agent 做，这件事就便宜了。**

### 分析四：agent 友好重排了整张优先级表

**如果编码主要由 agent 完成，几个权重会翻转：**

- **工具链成本几乎归零。** agent 不在乎编译一下。刚修好的「编写门槛」那行在 agent
  场景下几乎不重要了——而这恰恰暴露出那张对比表是按**人类用户**假设写的。
- **「即写即跑」权重下降**，真正要紧的变成**机器可读的错误**和**机器可验证的状态**。

**而最关键的一点：**

> **本项目的验证纪律本身就是一个 agent 安全特性。**
>
> `已验证`（真机跑过）与 `完成`（代码就位）这个区分，对人是自律，**对 agent 是护栏**
> ——它阻止一个 agent 把没测过的代码当成能用的。LuaTOS 和 MicroPython 没有这个
> 区分，因为它们不需要：人会自己去试。**agent 不会，除非你告诉它这里有区别。**

具体该做的事：错误要指名原因（已做）、设备要可自省（`adc`/`spi`/`pins`，已做）、
构建期拒绝要给理由（B2，已做）、**输出要机器可解析**（未做，见 A4）。

### 排序准则（先定死，再排任务）

**依赖优先，不按「便宜」或「显眼」插队。** 一个任务排在前面，只因为后面的任务
做不了，不因为它容易或者收益显眼。

> **这条是被自己违反之后补上的。** 计划刚排完，我就建议「A4/A5 不依赖多槽位，
> 可以先做」——那是换了个标准（独立且便宜）去推翻按依赖排好的顺序。
> **两个标准混用，顺序就失去意义。**
>
> 而且那个建议本身是错的：A4 是给 `status`/`pins` 加结构化输出，
> 而 **S7 正是「status 按槽位分列」**——先做 A4 等于等着被 S7 重写。
> 先做不省事，要做两次。

**例外只有一种**：某任务暴露了前序任务的设计错误。那种情况下**改的是计划本身**，
并在此处记明原因，而不是临时跳过去做。

### 任务表

**S0 — 阻塞其余一切。**

| # | 任务 | 说明 |
|:---:|---|---|
| **S0** | **host API 需要知道调用者是谁** | 现在 `host_i2c.c`/`host_uart.c`/`host_adc.c`/`host_spi.c` 里的 `checked_*()` 全都直接读 `g_mdl_slot` 这个**单例**。多槽位下「这条总线是你声明的」这句话必须先能回答「你是谁」。可行解：调用发生在该 MDL 自己的任务里，用 `xTaskGetCurrentTaskHandle()` 反查槽位。**这是最深的一处改动，其余任务都压在它上面。** |

**S1 — 没有这些就不叫多槽位。**

| # | 任务 | 说明 |
|:---:|---|---|
| S1 | **buddy 分配器** + **逐槽位分配器状态** | 2 的幂对齐是 MPU 的硬性要求，而那正是 buddy 的前提。释放时自动合并，粒度是「一个 MDL 一块」所以不需要追踪指针。内部碎片如实报告，不做整理。<br>**前置发现（S0 期间）**：`host_api.c` 的 `g_free_list`/`g_pool_next`/`g_pool_end` 是模块级静态量，即「一个分配器服务一个模块」。单槽位下正确，**两个槽位就错**——它们会跨各自的 arena 共用一条空闲链。 |
| S2 | **跨槽位资源仲裁（MDL ↔ MDL）** | 现在的冲突只有「MDL vs host」。多槽位后出现「MDL vs MDL」，而 R3 的引脚展开机制**已经覆盖这个语义**——需要的是把比对范围从一个槽位扩到全部。**这是多槽位最容易出安全问题的地方**：两个 MDL 同时驱动一根线 |
| S3 | 事件路由到正确的槽位 | `host_events.c` 现在只有一个 `s_module_task`。GPIO 边沿该投给**声明了那个引脚**的槽位，不是广播 |

> **计划更正（2026-09-14，做 S2 时发现）：S2 与 S3 在 supervisor 这里分不开，S3 是前置而非后续。**
>
> `reclaim_slot()` 里有一句 `mdl_events_disarm_all()`。单槽位时正确；**多槽位下回收
> 槽位 A 会把槽位 B 的中断一并下掉**。而这不是加个参数能补的——`host_events.c` 只有
> 一个 `s_module_task`、一个队列、一份 `s_armed_lines`，要让 disarm 只作用于一个槽位，
> 就得先有 S3 的路由。
>
> 我排序时把「事件路由」放在「多槽位加载」之后，理由是前者听起来像后者的完善。
> **实际是反的**：没有路由，多槽位加载一落地就带着一个真实缺陷（卸载一个模块会
> 悄悄让另一个模块的按键失灵）。
>
> 按排序准则的例外条款处理：**改计划，记明原因，而不是绕过去或先欠着**。
> S2 的 supervisor 部分与 S3 合并为一步完成。

**S2 — 使之可用。**

| # | 任务 | 说明 |
|:---:|---|---|
| S4 | 控制台命令名跨槽位唯一 | 两个 MDL 都叫 `run` 就没法派发了。加载期拒绝重名 |
| S5 | F2 持久化一组而非一个 | flash 末 16K 现在存单个镜像；改为存一组，并在启动时按序恢复 |
| S6 | 逐槽位看门狗与 parking | `parked_until_tick` 现在是单例字段 |
| S7 | `status` / `pins` 按槽位分列 | 一个槽位的输出现在是隐含的「那个」MDL |

**S3 — 即写即跑（明确不含 REPL）。**

| # | 任务 | 说明 |
|:---:|---|---|
| W1 | watch 模式：存盘即重建推送 | 把 715 ms 变成无感。`watch.py` 已有雏形，缺的是文件监听 + 失败不推送 |
| W2 | 控制台扩成硬件 shell | `i2c read 1 0x48 2`、`spi xfer 3 9F 00 00 00`、`gpio set 0 1`。覆盖 REPL 在嵌入式上的主要用途，**不需要解释器** |
| S8 | **解释器作为一个可选 MDL** | 化解 REPL 取舍的那个选项。Lua 比 Python 小得多，是更现实的第一步。不加载时成本为零 |
| — | ~~内置 REPL~~ | **明确不做。** 把解释器烧进固件等于让所有人替少数人的交互需求付性能代价。做成 MDL 则是逐模块选择 |

**S4 — agent 优先（按第四条分析，这一档的权重高于原先预估）。**

| # | 任务 | 说明 |
|:---:|---|---|
| A4 | 设备输出可机器解析 | `status` / `pins` / `adc` / `spi` 现在是给人读的。加一个结构化模式（JSON），让 agent 不必解析自然语言 |
| A5 | MCP 暴露**验证状态** | agent 需要能问「这个结论是真机验过的还是只是编译过」。这是本项目独有的护栏，现在只存在于 maintain.md 的文字里 |
| A6 | 驱动移植脚手架 | 承接分析三：给一个从 MicroPython / LuaTOS 的 C 驱动翻译过来的骨架 + 检查清单，让 agent 批量移植器件驱动 |

> **用户视角的验收**（分析三的落点）：装一个新器件的体验应该是
> **「推一个 .mdl，立刻能用」——不重启、不重跑、不重烧**。
> 库的价值最终体现在这里，而不是体现在能不能跑别人的源码。

### 验收标准

沿用本项目一贯的规矩：**编译通过不算**。2.0 完成的定义是真机上同时跑起
**ADC 采集 + SPI 记录**两个独立 MDL，各自声明各自的引脚，第三个 MDL 想抢已被占用的
引脚时**在构建期就被拒**，并且其中一个崩溃时另一个**继续运行**。

最后那条是多槽位真正的价值——也是它比单槽位难的全部原因。

**S8 的验收另计**：一个解释器 MDL 与一个原生 MDL 同时驻留，解释器崩溃时原生的
那个继续跑——那就同时证明了隔离和「逐模块选择语言」这两件事。

---

## 9. 工作日志

> 新条目加在最上面。格式：`### YYYY-MM-DD 名字/agent id` + 简短条目列表。

### 2026-09-14 claude/opus-5（S5：持久化一组而非一个——双 bank）

**「保存的那个 MDL」在四槽位下不再是一个说法。** 一块板子重新上电后跑着它原有三个
模块中的一个，不叫「扛过了掉电」，叫**丢了三分之二而且没吭声**。

**为什么必须做成双 bank。** 原来的保存是「全擦 → 写镜像 → 写头」。往一组里追加不能
这么干：**已有的条目就住在将要被擦掉的那些扇区里**，得先复制到别处。

所以 16K 拆成两个 8K bank。保存时把**整个新集合**写进**非活动** bank，
活动 bank 全程可读；写完才轮到它成为活动的。任何时刻断电，**上一组都完好无损**
——半写的 bank 永远拿不到它的 magic。

> **`seq` 是用来打破平局的。** 没有它，「写完新 bank」和「擦掉旧 bank」之间断电，
> 会留下两个都有效的 bank 而无从判断谁更新——**那正是这套方案存在要防的唯一失效模式**。

**按名字替换**，和 supervisor 选槽位用同一条规则、同一个理由。名字从镜像自己的
头部读，而不是由调用方传入：**一个模块的身份只有一个定义**，store 的条目选择
不可能和 supervisor 的槽位选择产生分歧。

用 `offsetof(mdl_header_t, name)` 而非手算常数——在 name 之前加一个字段会**静默**
移位，而故障表现是「按名替换不再匹配」：flash 里重复条目越堆越多，没有任何报错。

**启动恢复整组**，每个都走普通加载路径（所以恢复的模块和推送的模块经历同样的
重定位与仲裁）。**某个存档变得不可加载（ABI 变了、引脚被前一个条目占了）时
报告并跳过，而不是中止整组**——丢一个模块不该赔上其余几个。

### 2026-09-14 claude/opus-5（**2.0 四条验收标准全部成立**；S4/S7 完成；隔离的最后一环曾经是断的）

**验收标准（§8.5 定的，编译通过不算）逐条兑现：**

| 标准 | 结果 |
|---|---|
| 两个独立 MDL 同时跑，各自声明各自引脚 | 三个同时驻留，各占独立 buddy 块 |
| 第三个抢已占引脚 → **构建期/加载期被拒** | `sw3_blink -> needs PA3, already held by sw3_irq` |
| 事件只投给声明者 | 触发 100 次：槽位0 收 100，槽位1 增量与对照组**完全相同** |
| **其中一个崩溃时另一个继续跑** | 见下 |

```
邻居越界写宿主内存 -> MPU 拦下 (cfsr=0x82 DACCVIOL+MMARVALID, mmfar=0x20000000)
板子没复位（build id 不变，控制台存活）
另外两个仍 RUNNING
幸存的 uart_echo 仍能响应命令，回环仍逐字节正确：4D 44 4C
```

**最后一行才是重点**——不是「还列在 RUNNING」，而是**邻居崩溃后功能完好**。

---

**而隔离的最后一环曾经是断的，是做 S4/S7 时顺手查出来的。**

```c
bool is_module_fault = g_mdl_slot.state != MDL_SLOT_EMPTY &&
                        arch_pc_in_range(pc, g_mdl_slot.text_lo, g_mdl_slot.text_hi);
```

槽位 1 的模块崩溃，PC 不在槽位 0 的 text 范围内 → **判成宿主 fault → 复位整块板子**。

> **崩溃被 MPU 成功隔离了，然后恢复动作把所有人一起带走。**
> 给每个模块独立 MPU 区域的全部意义就是隔离，而它在最后一步失效。

**这是同一个错误在本次重构里的第五次出现**：单槽位时期写的代码继续读那个单例，
**编译零警告，静悄悄地意味着「槽位 0」**。五处分别是：trampoline 的 9 个辅助函数、
`host_gpio_yielded_to_module()`、`request_unload()`、控制台命令派发、fault 分类。

**S4 命令名跨槽位唯一**：`find_slot_by_command()` 返回第一个匹配，两个模块都叫 `run`
的话敲下去就是掷硬币**而且无声**（两次加载都成功了）。现在第二次加载被拒并指名占用者。

**S7 `pins` 报出具体模块**：四个槽位下 `[MDL]` 不再是答案，加载被拒时真正有用的
问题是**「哪个邻居占着」**。

### 2026-09-14 claude/opus-5（S2/S3 真机验证——多槽位成立，并抓到一整类我自己引入的 bug）

**真机结果：**

```
0: sw3_irq              text 0x20020000  claims 2  RUNNING
1: uart_echo   cmd uart text 0x20020800  claims 1  RUNNING
2: i2c_scan cmd i2cscan text 0x20021800  claims 1  RUNNING

sw3_blink -> ERROR: needs PA3, already held by sw3_irq
i2c_scan 再推 -> OK，仍是 3 个（同名替换而非新增）
```

**逐槽位事件路由（受控测量）：**

```
对照组（不触发 GPIO）：每次调用 uart 自增 (6,3)  <- 它自己发的 ping
实验组（触发 100 次 SW3）：
   槽位0 (声明了 SW3)  增量 (1, 99) = 恰好 100 个边沿
   槽位1 (没有声明)    增量 (6, 3)  = 与对照组完全相同
```

**100 次中断对槽位 1 的贡献是零。** 而第一次测量被混淆因素污染过——`uart` 命令自己
会发 ping，**读取动作改变了被读的量**。先测出对照组自增量才能把它减掉。
这个结论按多少下按键都得不到。

---

**而多槽位一上真机就抓到一整类 bug，全是我自己引入的，形状完全相同。**

| 处 | 症状 |
|---|---|
| `module_task.c` 的 9 个 trampoline 辅助函数 | 两次推送都 OK，`slots` 却显示 0 个驻留 |
| `host_gpio_yielded_to_module()` | 槽位1 的模块认领 LED，宿主仍继续驱动它——**一根线两个主人** |
| `mdl_supervisor_request_unload()` | 只卸载槽位 0 |
| **`console.c` 的命令派发** | `slots` 列着 `cmd uart`，敲 `uart` 却说 unknown |

**共同根因：过渡别名 `g_mdl_slot` 在已经多槽位化的代码里静悄悄地意味着「槽位 0」，
而且照样编译通过、没有任何警告。**

最后一处尤其值得记：supervisor 里的 `find_slot_by_command()` **写好了、是对的、
从来没被调用到**——因为控制台在派发前自己又比对了一遍 `g_mdl_slot.cmd_name`。

> **一个决定被复制成两份，单槽位时两份一致，多槽位时副本退化成「槽位 0」。**
> 这和 PE3 那次（GPIO 白名单在两张表里只改了一张）是同一个毛病。
> 已删掉重复判断：**一个决定只有一个地方做**，控制台只负责报告 supervisor 的答复。

**这正是「编译通过不算完成」的第 N 次兑现**：四处 bug，六个目标全部编译通过、
零警告，全靠真机上两个模块并存才暴露。

### 2026-09-14 claude/opus-5（`evt`：软件触发中断，让事件路径不再需要人按按键）

**用户提的：能不能设计一个自己交互的 MDL 测试，不用人按按键。** 正中要害——
而且它测到的正是刚重写、却毫无真机验证的那部分。

用 EXINT 自带的软件触发寄存器（`exint_software_interrupt_event_generate`），
**走的是和真实边沿完全相同的路径**：同一条 EXINT 线、同一个向量、同一个 ISR、
同一个队列、同一次任务唤醒。唯一不能证明的是「焊盘连着」，而那件事按键已经证明过。

**宿主专用，vtable 里没有入口**——能伪造自己中断的模块，可以伪造任何建立在中断
之上的东西。

按键测试的三个问题这里全解决：可重复、可无人值守、**分得清「链路坏」和「没人按」**。
而第四条最关键：**逐槽位路由根本没法用按键测**——你必须能「触发 A 声明的引脚，
再检查 B 一无所获」，按多少下都证明不了。

**真机结果：**

```
基线           0 delivered,   0 merged, 0 LOST
evt 2 x50      1 delivered,  49 merged, 0 LOST    <- SW3，已声明
evt 3 x50      1 delivered,  49 merged, 0 LOST    <- SW4，未声明：完全没变
evt 2 x200     2 delivered, 248 merged, 0 LOST, peak 50/s
```

**250 个边沿 = 2 投递 + 248 合并 + 0 丢失，一个不差。** 合并计数是精确的而非估算——
这正是当初坚持「合并数随事件带走」的意义：不这样的话，mitigation 就变成了 corruption。

**但要把话说准。** 这证明了软件触发有效、合并精确、未声明引脚产生不了事件。
**它还没证明跨槽位路由**：现在只有一个槽位在用，`evt 3` 什么也没发生可能只是因为
EXINT2 压根没使能（没声明就不会 arm），而不是「ISR 跑了但路由到了没人」。
两者结果相同、机制不同。**真正的路由验证要等两个模块同时驻留。**

### 2026-09-14 claude/opus-5（S2 槽位表 + 跨槽位仲裁；S3 逐槽位事件层——并更正了计划的排序）

**槽位表（`MDL_MAX_SLOTS = 4`）。** 数量是**策略而非硬件限制**：MPU 区域是每任务的、
切换时重编程，所以它不限制驻留数；限制来自 arena，4 个 10K 模块对 128K 池很宽裕。

**过渡宏刻意做得难看**（`#define g_mdl_slot (g_mdl_slots[0])`），这样它读起来就是临时的。
160 处引用里大部分属于单模块目标（M0–M3、QEMU 自检），它们本来就是单模块设计；
**真正需要多槽位感知的——调用者身份、仲裁、supervisor、事件——走整张表**。
一次性重命名 160 处、其中包含 H12 出过事的 MPU 设置代码，是一个没法审阅的改动。

> S0 把 `mdl_caller_slot()` 写成查找而不是 `return &g_mdl_slot`，**正是为了这一刻**：
> 槽位表变成数组时，它的调用者一个都不用改。

**跨槽位仲裁。** R3 的引脚展开语义本来就对，改的是**搜索宽度而不是语义**。
带 `exclude` 参数——热替换时被替换的模块还持有引脚，不排除它就会和自己冲突。

---

**而做 S2 时撞上计划本身的排序错误，按例外条款改了计划。**

`reclaim_slot()` 里有 `mdl_events_disarm_all()`。单槽位正确；**多槽位下回收槽位 A
会把槽位 B 的中断一并下掉，而且是静悄悄的**。这不是加个参数能补的——`host_events.c`
只有一个 `s_module_task`、一个队列、一份 `s_armed_lines`。

> 我把「事件路由」（S3）排在「多槽位加载」（S2）之后，**理由是前者听起来像后者的完善。
> 实际是反的**：没有路由，多槽位加载一落地就带着一个真实缺陷——卸载一个模块会让
> 另一个模块的按键失灵，而故障现场离原因很远。
>
> **改计划、记明原因，而不是绕过去或先欠着。** S3 提前，与 S2 的 supervisor 合并。

**事件层现在逐槽位**：

| 原来 | 现在 | 不改会怎样 |
|---|---|---|
| 一个队列 | 每槽一个（4 × 16） | 一个模块能读到邻居的事件 |
| 一个 `s_module_task` | 每槽一个 | 边沿唤醒**最后注册**的那个模块 |
| 一份 `s_armed_lines` | 每槽一份 | 卸载一个模块静悄悄关掉另一个的中断 |

**路由用一张 `pin → slot` 平表**，arm/disarm 时从任务上下文写、ISR 只读——
**中断处理程序不该去遍历加载器的数据结构**。`mdl_events_init()` 必须在启动时调：
映射表初值全 0 会读作「槽位 0 拥有所有引脚」而非「没人拥有」。

`mdl_events_take()` 用**调用者身份**取自己的队列——模块传槽位号的话，那就是个
模块可以撒谎的槽位号。与 S0 建立的原则一致。

真机：全量回归零变化，事件参数正确落位（`declared 50/s` 与模块声明一致）。
单模块下逐槽位与单槽位等价，这正是要确认的。

**S2 剩余**：supervisor 仍只往槽位 0 加载。下一步会带来**可见的语义变化**
（不同名模块并存而非互相替换），届时单独说明。

### 2026-09-13 claude/opus-5（S1 收尾：加载器改用 buddy 分配，按模块实际大小取内存）

**这是让"一个槽位"从结构性限制变回临时状态的那一步。** 加载器原先写进链接脚本
划死的三块固定区域，所以第二个模块无处可去——与其余代码怎么写无关。

**三块而非一块**，因为它们权限不同而 MPU 按区域施加权限：text（只读可执行）、
data（读写不可执行：GOT + .data + .bss）、heapstack（arg block + 堆 + guard + 栈）。
三块都直接来自 `mdl_buddy_alloc()`——**这里没有任何对齐运算，因为无事可做**：
它返回的每一块本来就是 2 的幂且对齐到自身尺寸，那正是 ARMv7-M MPU 区域的全部要求。

**尺寸是模块的，不是 arena 的**：text/data 取自镜像头部，heapstack 固定
（ABI 向每个模块承诺 `MDL_HEAP_SIZE`/`MDL_STACK_SIZE`，那是契约不是测量）。

| 模块 | text | data 区 | 合计（含 8K heapstack） |
|---|---|---|---|
| `sw3_blue` | 1K | 1K | **10K** |
| `spi_flash` | 2K | 1K | **11K** |

旧布局一律 **32K 且只能装一个**。128K 池现在放得下十来个。

**真机验证（三轮加载/卸载）**：加载后 arena 117~118K free；**卸载后每一轮都回到
128K free / 最大 128K**。后半句才是关键——不是"字节数回来了"，而是**最大块回到
128K**，那是合并真正生效的证明。只合并一半的话最大块会停在 64K，几轮后就装不下
大模块了。

**而这轮差点悄悄废掉一个测试。**

`sandbox_bounds_init()` 原先在启动时把边界填成链接脚本的固定区域；改成动态分配后
我把它清零了（空槽位不该看起来拥有 32K 内存——`ptr_owned_by_module()` 和 fault
分类都读这些字段）。

> **但 M0 的自检是读 `g_mdl_slot.guard_lo` 然后故意踩它**，证明 MPU 会拦住。
> 清零之后它会去碰**地址 0**——**仍然 fault、仍然"通过"，而证明的东西没了**。
>
> **这是最阴的一种回归：测试还绿着，测的东西已经不在。**

改成直接读链接符号 `__mdl_guard_start`。它本来就该测 `sandbox_init()` 建立的那个
**静态**区域，而不是某个模块的**动态**边界——两者之前恰好相等，所以一直没被区分。

**其余刻意决定**：acquire 全有或全无（部分获取会让没加载成功的模块占着块，那种
泄漏只表现为"arena 莫名变小"）；`release` 清空边界而不只是清标志（残留的 text_lo
对 `ptr_owned_by_module()` 仍像一个有效区域，而它指向的内存已归下一个模块）；
校验仍是纯的（F3 要求），只问装不装得下，真正易手在 acquire。

**S1 完成。**

### 2026-09-13 claude/opus-5（S1：buddy 分配器 + 逐槽位堆；并抓到 free() 从未真正释放过）

**分配器本体。** MPU 要求区域 2 的幂且自然对齐——这看似负担，**却正好是 buddy
的前提条件**，于是这个约束不花钱，还买回最要紧的性质：**释放时合并**。
卸载一个模块交还的是可用的大块，而不是碎渣。

几个决定：

| | 为什么 |
|---|---|
| **侧表而非块头** | 返回的指针必须是自然对齐的块基址——那是用 buddy 的全部理由。块头会把可用区推离对齐，把目的抵消掉 |
| **取能满足的最小阶** | 拿 128K 块服务 1K 请求，是让 arena 从此装不下大东西的标准做法 |
| **坏的 free 被忽略而非执行** | 破坏分配器比泄漏一块糟糕得多；重复释放是调用者的 bug，不该升级成内存损坏 |

arena 从 64K 固定四段改成 **128K 单一对齐池**（旧布局一半是对齐填充且只能装一个
模块）。RAM 剩余约 188K。

**自检在真机上跑**——主机单元测试能证明逻辑，**证明不了它在这个目标、这份固件、
这套 MPU 配置下的行为**，而那才是本项目反复被咬的地方。检查的是设计真正依赖的性质：
每块对齐到**自己的尺寸**（不是最小单位）、**全部释放后池是一整块**（字节数回来不算，
碎片化恰恰是要避免的状态）、坏 free 不破坏结构、耗尽干净拒绝。真机两次结果一致。

**逐槽位堆状态**：`g_free_list`/`g_pool_next`/`g_pool_end` 三个模块级静态量搬进
`module_t`。两个模块绝不能共用一条空闲链——它们的 arena 是不同的内存，
**一个模块的块交给另一个就是一个直接穿过沙箱边界的指针**。

---

**而为此补的测试模块当场抓到一个一直存在的真 bug：`free()` 从来没有真正释放过。**

```c
b->size = 0;    ← 每个被释放的块都记成 0 字节
```

而 `alloc` 的复用条件是 `b->size >= n`——**`0 >= n` 对任何 n > 0 都不成立**。
空闲链是只写的，池只能单向消耗到耗尽。

根因：**分配器从不记录已分配块的大小**。`alloc(n)` 把指针加 n 就返回，没人记住 n，
于是 `free(p)` 无从知道该还多大，`size = 0` 是个让块永久不可用的占位符。

> **跑一次看不出来**：第一次 8 块、第二次 4 块，只有连着跑两次才露出来。
> 而在写这个测试之前，**没有任何测试 MDL 调用过 `alloc`/`free`**——
> 改了分配器却零覆盖就宣布完成，正是「编译通过不算完成」这条规矩存在的理由。

修法是 8 字节块头。**这在 `buddy.c` 里绝不能做**（那里返回的指针必须是 MPU 对齐的
块基址），但模块堆的块在**已对齐的 data 区内部**、永不交给 MPU 区域，所以偏移
8 字节没有代价；8 而非 4 保持返回指针 8 字节对齐，调用者放 `double` 时有权假设。

测试也改成**把所有块还回去**，于是连跑多次数字必须一致——直接针对那个 bug 真正
表现出来的症状。真机三次全部 `8 x 64B / 6 x 512B`，`free() feeds alloc()`。

**S1 剩余**：加载器仍用链接脚本的固定符号，尚未切到 `mdl_buddy_alloc()`。

### 2026-09-13 claude/opus-5（S0 完成；并由此挖出 I2C 从来没真正工作过）

**S0：host API 认得调用者是谁。**

身份用**任务句柄**而不是让 MDL 自报槽位 id——这是安全相关的选择：ABI 里传 id 的话
**MDL 就能声称自己是别人**。任务句柄由内核分配、由宿主读取，模块碰不到也伪造不了。

改成问调用者的有五处，每一处在多槽位下都是真实漏洞：

| | 单槽位 | 多槽位下不改的后果 |
|---|---|---|
| `checked_*()` ×4 | 隐含答案 | 用别人的声明批准你的传输 |
| `ptr_owned_by_module` | 同一块内存 | **一个 MDL 递来的指针，拿另一个 MDL 的 arena 校验并放行** |
| `module_claimed` | 同上 | 驱动别人认领的引脚 |
| 喂狗 / parking | 只有一个 | **忙的 MDL 替卡死的 MDL 喂狗，把它一直续命** |

「这个 MDL 声明过这个资源吗」收成**一份实现**供四个驱动共用——四份几乎相同的权限
检查迟早走样，而**走样的权限检查就是洞**（PE3 那次两张表只改一张是同一个毛病）。

真机回归全绿、**行为零变化**——单槽位下调用者恒为那一个，结果必须完全一致。

**S1 前置发现**：`host_api.c` 的 `g_free_list`/`g_pool_next`/`g_pool_end` 是模块级静态量，
两个槽位会跨各自 arena 共用一条空闲链。**没有顺手修**——那是 S1 的范围，已记进条目。

---

**而回归顺带暴露了 I2C 从来没真正工作过。** 三层问题，逐层剥开：

**第一层：误报。** 扫描报出一个器件，但**地址每次都变**（0x68 → 0x60 → 0x60）。
真器件不会换地址。111 次失败探测的脏状态渗进了下一次，在随机位置被读成成功。
vendor 库清了 ACKFAIL、返回码也对，但**不保证失败后总线回到空闲**。
加了 `bus_recover()`，只走错误路径。

> 扫描是把这个放大的极端用法，但**应用会更安静地撞上同一件事**：一个不在的传感器
> NACK 一次，紧接着对一个**真在**的器件的传输就返回垃圾——那种故障会被叫"间歇性"
> 查上一周。

**第二层：还是扫不到。** 而这次有个关键数字——**112 次超时、0 次 NACK**。
那是「传输从没开始」的特征，不是「总线是空的」：器件在不在都该是 NACK。
（这个区分是临时加进扫描模块的；在此之前两者被折叠成同一个「没器件」，
和 I2C1 配错引脚被藏三轮是同一个毛病。）

逐项排除：引脚配置从寄存器读回全对（MUX=4/开漏/上拉/空闲高）；时序字按例程 PLL
算下来 APB1 确实 144MHz、`0x80504C4E` 正确；外设 BUSYF=0、使能位=1；
`error_code = 18 = I2C_ERR_TIMEOUT`。

**第三层，根因，是我自己埋的。** vendor 的 `i2c_wait_flag`/`i2c_wait_end` 把 timeout
参数当作**裸自减自旋计数**：

```c
while (i2c_flag_get(...) != RESET) {
    if ((timeout--) == 0) { ... return I2C_ERR_TIMEOUT; }
}
```

**它没有时间单位。** 例程传 `0xFFFFFFF`。**我传的是 100**——288MHz 下不到一微秒，
而 100kHz 发一个字节要 90µs。**每次传输都在总线来得及做任何事之前就放弃了。**

而我的常数叫 **`I2C_TIMEOUT_MS`**，注释写着 **"Milliseconds, per the vendor API"**。

> **我给一个没有单位的参数起名字赋予了单位，此后这个名字就成了那个论断唯一的
> 证据**——包括对重读自己代码的我。改名 `I2C_TIMEOUT_SPINS`，并写明 2,000,000
> ≈288MHz 下 100ms 是**量级估计不是保证**。

**修复后真机结果：**

```
device at 0x50          ← 原理图里 AT24C64 (U8) 该在的地址，三次扫描一致
device at 0x58          ← 稳定因而真实，但原理图文本里没找到对应器件，未识别
@0x0000: 44 61 74 61 20 6E 6F 77   = ASCII "Data now"
@0x0100: FF FF FF FF FF FF FF FF   = 未写过
```

两个地址给出不同内容，说明**寻址真的在工作**；可读 ASCII 不可能是巧合。
**I2C 至此完整验证**：扫描、寻址、重复起始位（当初论证过必要却一直没跑过的那条
路径）、从真实器件读到真实数据。

**遗留**：不存在的地址仍然是超时而非 NACK，所以全量扫描要 35 秒。对正常应用无影响
（器件在的时候一切正常），但扫描慢，且 NACK/超时这个判别器没有预期中好用。已知，
未修。

### 2026-09-13 claude/opus-5（更正对比表里一句把本项目设计写反的话）

用户指出对比表「编写门槛」那行写着**「改一行要重新编译打包」**——读起来像是每改一个
功能都要把所有功能重新打成一个总包。**那正是本项目明确拒绝的设计。**

核实后的事实：

| | |
|---|---|
| 每个 MDL 独立编译打包 | `i2c_scan`：编译+链接+打包 **270 ms**，产物 **1024 B** |
| 改哪个推哪个 | 不重编固件，也不碰其他 MDL |
| 但同时只能驻留一个 | `g_mdl_slot` 是单数，推新的替换旧的（F3 原子替换） |

**我把两件事搅在了一起**，结果一句话既说错了独立编译这件对的事，又漏掉了单槽位
这个真实限制。已拆成三行：**更新粒度**（我们和它们其实一样是单文件单独上传）、
**同时驻留**（它们多模块共存，我们目前一次一个——这是未做的工作，不是设计取舍）、
**编写门槛**（真正的差距：主机要有工具链、设备上没有 REPL，循环是"改→编译→推"
而不是"改→跑"）。

> 值得记的地方在于**错的方向**：这次不是把自己说弱了，是**把自己的架构描述错了**。
> 一张对比表最容易出错的地方恰恰是自己那一列——对别人那两列我反复检查了"有没有
> 测过"，对自己这列却凭印象写了一句。

### 2026-09-13 claude/opus-5（SPI / ABI v10，外设序列收官 + 原理图推翻三处板级假设）

**用户提供了 UYUP-RPI-A-2.4 原理图，它一次解决了一个反复发生的问题。**

`board_pins.def` 新增 **53 条 `MDL_HEADER_PIN`**，记录 U5/U6 两个 2x20 排针引出的
每一个脚。这张表回答的问题和其他表**不同**——其他表说「这个脚归谁」，它说
**「够不够得着」**。而后者被我猜了三次，每次花掉一轮真机测试：
USART2 的 RX（PA3，只通按键）、SPI3 的 PC10/PC11/PC12（microSD 插座）。
两次都**芯片上完全正确、板子上彻底碰不到**。

> **「芯片上确认过」和「板子上够得着」是两个不同的断言。** 把它们混为一谈是这轮
> 之前所有弯路的共同根因。现在 `mdl_periph_unreachable_pin()` 能直接回答。

**同时查出两处我们一直记错的引脚：**

| | 我们记的 | 板子实际 | 后果 |
|---|---|---|---|
| I2C1 | PB6/PB7 | **PB8/PB9**（带 10k 上拉 R44/R45） | PB6/PB7 在这板上是摄像头接口、**无上拉**。那次"空总线扫描干净返回"驱动的是一条不可能工作的总线——没接器件时空总线和错总线读数一模一样，所以没暴露 |
| USART2 | PA2/PA3 | **PD5/PD6**（均在 U5） | **这才是接收三轮不可测的真正原因**——不是芯片行为，是配在了一组这块板没那么用的脚上 |

**SPI（ABI v10）**：原语是**一次全双工传输**，不是 write/read 分开——SPI 物理上就是
同一个时钟沿移出一位移入一位，拆开是在发明线上不存在的区分。片选不归驱动管
（每器件时序不同，写死的策略对三分之一器件是错的），速度**向下取整而非取最近**
（跑慢从不弄坏器件；跑快会千分之一出错，而那种故障被归咎于接线查一周）。

**测试用已知答案而非回环**：SPI3 上挂着板载 SPI NOR Flash，读 JEDEC ID。
回环只证明外设能跟自己说话；**读 ID 证明总线够到了真器件、时钟极性对、片选框对、
字节序对**——因为只有一个特定答案能通过。时钟相位错、MISO/MOSI 反、mux 错、
器件不存在，这四种**全能混过回环**。

真机：`C8 40 16` → GigaDevice GD25Q32，4MB，两次读取完全一致。
**顺带确认了 PB3/4/5 的 `GPIO_MUX_6`**（那是从 PC10/11/12 例程推过来的、没有例程
确认；没有别的 mux 能得到合法 JEDEC 三字节）。

**测试自己写窄了一次**：我硬编码 Winbond 的 `0xEF`，把一条完全正常的总线判成
`unexpected id`。板上装的是 GD25Q32——而**原理图在 W25Q32 旁边就写着「或其它相
兼容的产品」，我读到了却没反映进判定**。改成判「这是不是一个合法的 SPI NOR ID」
（厂商字节非 0x00 非 0xFF + 容量能解码成合理大小），那才是该测的东西。

**另一个结构缺陷：GPIO 白名单存在于两个表。** `board_pins.def` 管仲裁（索引↔物理脚），
`host_api.c` 管驱动（端口/掩码/时钟/方向/EXINT）。给 PE3 只加了前者，结果是
**仲裁层放行了声明、驱动层却不认识这个索引**。已补齐并在代码里写明必须逐索引一致。

**还补了一个我自己留的坑**：资源表里登记了 USART2 instance 4（PD5/PD6）却没在驱动
里实现——声明能过仲裁、claim 必然失败。现已实现，且因为 instance 2 和 4 是同一个
外设的两套引脚，中断向量改为**按外设查而非按数组下标查**（这张表已重排过一次，
写死下标会把别的总线的字节投递错），并拒绝同一外设在另一套引脚上的第二次 claim。

**外设序列收官**：GPIO / I2C / UART / ADC / SPI 五项全部真机验证。

### 2026-09-13 claude/opus-5（ADC / ABI v9 —— 真机跟随触发信号验证通过）

**形态与 I2C/UART 都不同：ADC 是一个外设带很多通道，不是很多实例。**
所以资源 id 是**通道号**而非实例号：`MDL_RES_ADC(4)` 在 host 侧展开成 PA4，
再和任何 GPIO 认领按**引脚**比对——正是 R3 的 pin expansion 该干的事。

这带来一个直接好处：**一个固件把 10 个通道全给出去**，不用再猜排针上有什么。
这条正是 USART2 那三轮换来的教训——当时整条接收路径不可测，只因为它的 RX 脚
（PA3）没引出，而我花了三轮去绕开一个引脚，而不是换一个。

给出的通道：CH0/1/4/5 → PA0/PA1/PA4/PA5（例程确认 `PA(n)→CH(n)`），
CH10..15 → PC0..PC5（数据手册，**未经例程确认**，控制台里明确标注）。
**故意不给** CH2/3/6/7/8/9——它们的引脚在同一张表里已属 USART2 与 TMR3，
给出来就是预装一个必然冲突。

新增 `adc` 控制台命令：**不写 MDL 也能问"该接哪个脚"**。替代方案是去读
`board_pins.def` 然后相信设备上的固件与它一致——而那正是 USART2 上栽过的假设。

**毫伏数不假设 3.3V**：用芯片内部 1.2V 基准（CH17）换算，`mV = raw × 1200 / ref`，
VDDA 直接约掉。板子实际 3.28V 而假设 3.3V，每个读数偏 0.6%——小、看不见，
且会让传感器永远显得"标定有点不准"。

**真机结果（用户持续触发 8 秒，采 401 次）：**

| | |
|---|---|
| 低电平 | raw 0..5（~0 mV） |
| 高电平 | raw ~4045（满量程 98.8%，若 VDDA=3.3V 约 3.26V） |
| 摆幅 | 4066 counts，约 17 次脉冲，周期 ~0.46s 规整 |
| 各态噪声 | 极差 3–5 counts（约 1 LSB） |

**这才排除了上一轮的歧义**：静止读数 ~0 分不清"器件输出 0V"和"根本没接上"。
只有让电压动起来、看它跟不跟，才是通道验证。

**踩到一个会重复发生的坑：`loader.c` 把合法 kind 上界硬编码成 `MDL_RES_KIND_TIMER`。**

ADC 是 kind=5，加载直接被拒 `malformed resource claim`。
没有只把 `TIMER` 改成 `ADC`，而是**加了 `MDL_RES_KIND_MAX` 哨兵让上界从枚举推导**——
否则下次做 SPI 会再踩一模一样的坑，而且 **编译期什么都发现不了**（`5 > 4` 是合法 C）。
顺带补了 `kind_name()` 里缺的 "adc"，否则冲突报告会把它叫 "unknown"。

### 2026-09-13 claude/opus-5（UART 接收已验证 —— 换串口，而不是继续修 PA3）

**用户一句话点破：「你换一个串口不就好了，短接的目的是不是为了回环」。**

是。回头看，之前三次弯路是同一个错误的三种形态——我一直在问
**「怎么把信号弄到 PA3 上」**，而该问的是 **「哪个 USART 的两个脚都引出来了」**：

| 弯路 | 结果 |
|---|---|
| 半双工自环 | 发送期间接收器不被触发（`dropped` 也是 0） |
| 按键当信号源 | 信号是真的，但需要人——空结果分不清"链路坏"和"没人按" |
| 建议短接 PA2–PA3 | **PA3 根本没引出**，是对另一块板子的建议 |

**改用 USART3（PB10 TX / PB11 RX）**，vendor `usart/interrupt` 与 `polling` 例程
确认过引脚与 `GPIO_MUX_7`（不是从 AF 表推的）。两脚相邻，一根跳线即**真全双工回环**：
不依赖任何芯片特殊行为、不需要第二个设备、不需要人按、谁都能复现。
USART2 保留在驱动里（它记录 PA3 与 SW3 的真实冲突，`self_clash` 靠它），
但注明它在这块板上永远做不了回环。

**真机结果（PB10–PB11 已短接）：**

```
wire test: sent MDL, got back 3 byte(s) 4D 44 4C  -- RECEIVE PATH VERIFIED
uart3: 16 bytes in 3 event(s), last: 0A
events : 11 delivered, 25 merged, 0 LOST   peak 16/s
```

ISR → 环形缓冲 → 事件 → `module_event()` → `read()` 全链路通，0 丢失。
**UART 接收从此是「已验证」。**

**第一次跑时测试自己看不出来它通了，这个 bug 值得记。**

输出自相矛盾：`wire test: received 0` 但累计计数是 `16 bytes`——而 **16 正是
`"MDL uart alive\r\n"` 的长度**，最后一字节 `0A` 就是那个 `\n`。

根因：`module_cmd()` 与 `module_event()` **由同一个常驻任务消费**（F1 的设计）。
所以命令里那句 `delay_ms(50)` 等的是一个**在它返回之前不可能被投递的事件**——
一个任务没法同时待在两个地方。**它在等一件由它自己的执行所阻止的事。**

> 改成直接 `uart_read()`（不阻塞、直接读 host 的环）。顺带一条通用教训：
> **同一屏上两个数字互相矛盾时，其中一个在量错的东西**——这次是测试，不是被测物。

### 2026-09-12 claude/opus-5（PA3 无引出 —— 推翻了一条我给错的测试建议）

**用户查原理图指出：PA3 没有引出到排针。** 那么我之前反复给的"短接 PA2–PA3 做
真环回"根本做不到——**那是对另一块板子的建议**。已在三处更正：`uart_echo` 注释、
进度页、以及 `board_pins.def`（把"PA3 无引出"记成板级事实，免得下次又有人从
"USART2 RX 是 PA3"推导出同一个不存在的方案）。

**这反而把按键那条路推回台面**，而且它从来没真正跑过。PA3 虽未引出，但它接着 SW3：
按下就是把 USART2 的 RX 拉低，那是一个起始位。之前否掉它的理由（机械边沿 vs
9600 波特 104µs 位宽）**算错了方向**——位宽是可以调的：

| 按下时长 | 1200 波特（位宽 833µs） |
|---|---|
| 极短 ~2–3ms | 起始位 + 几个低数据位 + 停止位为高 → **成帧为真实字节** |
| 正常轻按 30ms+ | 停止位处仍为低 → **framing error**（host 计入 `dropped`） |

**两种结果都是证据。** 只有"字节 0 且错误也 0"才说明接收器压根没被触发——而那正是
一直以来测到的。1200 是 host 允许的下限，所以**不需要重新烧录**，换个 MDL 即可。

**测试形态也改了：两段式，让它无法说谎。**

先加载 `sw3_irq` 用 EXINT 证明按键确实被按下，再换 `uart_echo` 看 USART2 收到什么。
没有这个见证阶段，"没人按"和"链路坏了"读数完全相同——我上一轮就又掉进了这个坑：
脚本把"请按 SW3"打在自己的 stdout 上，而**用户根本看不到那行字**。

本轮实测：Phase 1 = 0 个边沿 → 判定 **"按键未被按下，Phase 2 不构成证据"**，
而不是报成接收失败。测试拒绝下结论，这是它该有的行为。

**UART 接收仍未验证**，且现在知道验证它只需要一件事：有人按住 SW3 的时候跑一次。

### 2026-09-12 claude/opus-5（全量回归复跑 —— 不烧录，验证既有结论仍然成立）

**进度页**：<https://claude.ai/code/artifact/70fe49e2-f28b-439e-8703-a45b00553e85>
（可交互台账：31 条按状态/类别筛选，含基准与未结项。本文档仍是唯一事实来源，
页面是它的视图——两边不一致时以本文档为准。）

固件未改（设备与本地 elf 同为 `Sep 8 2026 17:57:12`），直接在真机上把**已验证的结论
全部复跑一遍**。目的是让进度报告有新鲜证据，而不是靠记忆背书——一个几天前验过的
结论，在没人再碰它的情况下悄悄失效，是这类项目最常见的退化方式。

| 复跑项 | 结果 |
|---|---|
| 三个 MDL 在同一按键上换行为，不重烧 | `sw3_blue` / `sw4_green` / `sw3_blink` 全部 OK |
| 资源冲突加载期拒绝并**指名引脚** | `conflict` → 点名 PA9 DAP 调试口；`self_clash` → 点名 USART2 与 BTN0 都要 PA3；`uart1_clash` → 点名 USART1 需要 PA9 |
| 坏镜像被拒时正在跑的 MDL 不受影响 | 垃圾镜像 → `ERROR image smaller than header`，随后 `status` 仍是 `RUNNING sw3_blue` |
| I2C 空总线扫描 | `found 0 device(s)`，112 个地址全部干净返回 |
| 引脚归属表 | host 两个指示灯标注 yields，SW3/SW4 free，PA9/PA10 标为永不可夺 |
| 基准（重取） | 空循环 **10.45**、原生循环 **12.45**、gated host 调用 **213.57** cycles |

派发延迟（1.7/4.6/5.9 µs，308 样本）**本次未复跑**——它需要人按 SW3。旧结论仍然挂在
那里，但按本文档的规矩，没复跑就不算本次验证过。

**UART 接收仍未验证。** 本次仍是 `sent 3, received 0`；PA2–PA3 未短接，符合预期，
不是新的失败。

### 2026-09-08 claude/opus-5（UART / ABI v8 —— 发送已验证，接收未验证）

**UART 的形态和 I2C 不同**：I2C 是事务式的，UART 是流式的——数据随时会来，
MDL 不在读的时候也得有人接着。这一条决定了其余全部：

- **接收走中断进环形缓冲**。只在 `read()` 期间收的话，两次调用之间到达的字节
  全丢且 MDL 无从知道。
- **`read()` 不阻塞、返回计数**。"读满 n 字节"是对线路做了它没答应的承诺；
  读方必须猜还会来多少，那正是串口协议死锁的经典成因。
- **溢出丢最新并计数**。带完整前缀的截断流还能解析，中间有洞的通常不能。
  静默丢字节的故障会被当成"这传感器不太稳定"查上一周。
- 事件复用 F1 的合并机制——UART 天然适合：**重要的是"有数据"，不是"发生了几次"**。

**验证到什么程度（不含糊）**

| | 状态 | 依据 |
|---|---|---|
| 配置 | **已验证** | 寄存器逐个读出：`baudr=0x3A98` → 精确 9600，REN/RDBFIEN/UEN 全置，NVIC IRQ38 使能 |
| 发送 | **已验证** | TDC 置位、返回成功、不阻塞（对端没接时该测的正是这个失败模式） |
| **接收** | **未验证** | 中断/环形缓冲/事件从未被真实数据触发过 |

**两次测试设计错误，都值得记**

1. **按键测试**：USART2 的 RX 是 PA3，而 PA3 就是 SW3。设想按键拉低 = 起始位。
   但我在用户还没按的情况下，把"收到 0 字节"写成了"实测边沿太慢不成帧"——
   **把没有证据的推测写成了实测结论**。已改成"未测试"。
   > 换掉它的真正理由不是它失败了，而是它**需要人**：不能重复、不能无人值守，
   > 而且分不清"接收链路坏了"和"没人按"——那正是当时浪费掉的那一轮。

2. **半双工自环**：试了两次（推挽 → 开漏，后者是单线模式的硬性要求），都是
   `sent 3, received 0`，且 **dropped 也是 0**——接收器根本没被触发，不是成帧失败。
   最可能是这颗 USART 在单线模式下发送期间断开接收器（多数芯片如此），但
   **那是我手头资料确认不了的芯片行为，所以写成猜测而不是结论**。

   函数保留（半双工对单总线器件是真需求，实现是对的），但**不再伪装成自检手段**。
   代码和输出都改成实测口径：`0` 不报"坏了"，而是说明这是预期行为并指出
   验证接收需要外部信号。

**要验证接收**：一根杜邦线短接 PA2–PA3（真正的全双工环回，不依赖任何芯片特殊
行为），或接 USB-TTL 模块，9600 8N1。

### 2026-09-08 claude/opus-5（I2C 驱动 / ABI v7，外设序列开始）

**H10：一半 AT32 已经答了，另一半问的不是同一件事**

QEMU 目标与 M4 用**同一份** `port.c` 和 `module_task.c`，H8/H11/H12 三个修复都在
这份共享代码里，而 AT32 真机上任务切换/加载/中断跑了几十轮——**PendSV 那个 bug
已经答完了，不需要 QEMU**。剩下"QEMU 目标本身能否运行"是**可移植性回归**，证明
`core/`/`arch/` 没偷偷依赖 AT32。

查这件事时发现它**已腐坏两处**：vendor 库 2026-09-06 搬家时它没跟着改；
`transport/protocol.c` 进了 C_SRCS 却没进 vpath，自那时起就没链接过。这是第三次
同类腐坏，根因每次相同：**`build.ps1 all` 只构建 HIL 目标**。已修，并把 `qemu`
加进默认集——**没人构建的目标就是已经坏了的目标**，而它的全部价值是证明架构层
可移植，不编译就证明不了。

**I2C 驱动（ABI v7）**：`i2c_write` / `i2c_read` / `i2c_write_read`

R3 给了 MDL **声明**外设的能力，这是**驱动**它的那一半。R3 的约束全部保留：
没声明过的总线，**每次调用**都拒，不只是加载时。

- MDL 碰不到外设寄存器（没有任何外设的 MPU 区域），所以传输只能是 gated 调用——
  而那恰好是唯一能强制"这条总线是你的""这个缓冲区在你自己内存里"的地方。
- **阻塞但有界**（100ms）。跑在 MDL 自己的任务里（系统最低优先级），只阻塞它自己。
  超时不可省：**"传感器没插"在现场是常态**。
- 返回码把 **-2（无应答）和 -3（传输出错）分开**——合并成 -1 会丢掉唯一决定
  "下一步查什么"的区别。
- **CLKCTRL 是按主频算的常数**，vendor 例程的值只对它自己的 APB1 成立。没有照抄，
  而是初始化时校验 APB1 == 144MHz：时钟错不会干净地失败，它会给你一条
  **大部分时候能用**的总线。
- `i2c_write_read` 用重复起始位，不插 STOP——很多器件在 STOP 时复位内部地址指针，
  两次调用的版本会**静默读到错误的寄存器**。

真机（空总线）：112 个地址扫完，全部干净报"无应答"，`found 0 device(s)`。
没挂死、没乱报——这正是没接器件时该有的结果。

**踩到一个真的设计缺陷：`ptr_owned_by_module()` 不含模块栈**

```c
ptr_in_range(addr, len, m->heap_stack_lo, m->heap_stack_lo + MDL_HEAP_SIZE)
                                          // 只有 heap，栈在 +4K+2K 之后
```

> **为什么现在才暴露**：v1–v6 的 vtable 里**只有 `log()` 收指针**，而模块传的都是
> 字符串字面量（.rodata，属 text 区）或全局变量（data 区）。**I2C 是第一个需要
> 调用方提供缓冲区的 API**，而写缓冲区最自然的方式就是局部变量——在栈上。
>
> 而且它错在**危险的反方向**：不是放行了不该放行的，是**拒绝了完全合法的调用**，
> 还用和真正越界一模一样的错误码。

已修（栈纳入范围；guard 带仍排除——它没有 MPU region，MDL 碰它就该 fault）。

**定位方式值得记**：`res_count`/`res[]`/`CRM_CFG`/`g_buses[0].open` 全部用 `mem`
从真机读出来逐一排除，没有一步是猜的。而促成这次定位的直接原因是我先修掉了自己
写的一个坏东西——`host_i2c_claim()` 失败时**静默返回 false**，那正是这个项目
反复批评、也反复被坑的"不给理由的拒绝"。

### 2026-09-08 claude/opus-5（P3：F3 原子热替换 —— P 序列清零）

真机三点全过：

```
断电重插后          slot : RUNNING sw3_irq        （F2 未被 F3 破坏）
推坏镜像 self_clash  RESP_ERROR: ... 都是 PA3
                    slot : RUNNING sw3_irq        ← 旧的毫发无损
推好镜像 sw4_green   RESP_OK，无需先 unload
                    [host] gpio released -> default: 0 2
                    slot : RUNNING sw4_green
```

**先说做不到的**：单槽单 arena，**零窗口不可能**——新镜像就是往旧镜像的内存上搬，
旧的必须先死。这个窗口消不掉，没有假装消掉。

能消掉的是**进入窗口的理由**。原来工具端是 `UNLOAD` → `LOAD` 两帧，中间设备完全
没有 MDL（引脚回默认、中断解除布防），而如果新镜像有问题，**旧功能已经没了，板子
就空在那儿**。现在：

1. 校验新镜像（magic/CRC/ABI/arch/尺寸/重定位/资源冲突）——**旧 MDL 照常运行**
2. 失败 → 报错返回，代价为零
3. 通过 → 才拆旧的，然后搬运 + 起任务（窗口只剩这一段）

**校验与实际加载共用同一份代码**（`mdl_load()` 内部调 `mdl_load_validate()`）。
分成两份实现迟早会出现"预检通过、加载器却拒绝"，那比不做预检更糟。

顺带把 `check_resources()` 改成**纯函数**：它原来把认领写进 slot，加载时无害，
**校验时就是污染正在运行的 MDL 的状态**。

窗口内起任务失败的话旧的确实没了，这种情况不装作没事——回复直接说
`the previous MDL is gone, power-cycle to restore the saved one`，F2 的 flash
store 就是恢复路径。

`mdl_supervisor_restore()` 也改成复用 `start_loaded_module()`：**恢复的 MDL 和
推送的 MDL 必须以同样方式起来**，否则那个差异只会在客户现场显形。

工具侧 `watch.py` 与 MCP `deploy_feature` 不再先 unload——那正是本次要消掉的东西。

---

**P 序列到此全部完成。** P0（R1/R2）、P1（H2/F1/R3）、P2（B2/P5/A1–A3）、
P3（F2/F3）。剩余未做项只有 H10（复跑 QEMU，本机未装）和 X1/X2（跨架构，X 序列）。

### 2026-09-08 claude/opus-5（P3：F2 掉电保持，真机验证）

断电重插后设备自己起来，**没有推任何东西**：

```
slot   : RUNNING   sw3_irq
events : 5 delivered, 1 merged, 0 LOST     ← 中断链路也一起恢复了
stored : 304 bytes at 0x0803C010
```

事件计数是关键证据：恢复的不是"一个装进去的镜像"，而是**完整功能**——GPIO 声明、
EXTI 布防、事件队列全部就位，因为开机走的是**同一条加载路径**，不是第二个只在
启动时执行、只在现场出问题的加载器。

**四个决定**
- **存原始镜像，不存 arena**。arena 里的内容已按运行时基址重定位、GOT 被改写，
  存下来只在它碰巧占的那个地址上有效。镜像才是可移植的东西。
- **先写镜像、最后写头**。掉电发生在中途，magic 就没写上，记录读出来是"不存在"
  而不是"一个指向半截镜像的合法头"——这是**开机空着**和**开机进垃圾**的区别。
- **跑起来之后才落盘**。存一个装得上但起不来的镜像，等于给板子一个每次开机都重演
  同一次失败的理由：一块会自我复制的砖。
- **加载失败不擦存储**，只报告。悄悄忘掉会把"设备为什么空着"这条线索一起抹掉。

链接器 FLASH 从 256K 缩到 240K，末 16K 划给 store——让重叠**物理上不可能**，
而不是依赖"顶上还空着 120K"这种链接器并不强制的性质。

**踩了两次同一类坑，值得记**

第一次：`flash_word_program()` 在这颗片子上就是**往 flash 地址存一个字**，而
FreeRTOS-MPU 把整个 flash 标成只读（对代码区完全正确）。于是落盘直接 DACCVIOL。
缩小 MPU 的 flash 区避不开——region 大小必须是 2 的幂，240K 向上取整回 256K。

第二次（更有价值）：我把 MPU 关掉罩住**整个**擦写过程，为了不掐 USB 特意保留中断
——**结果一模一样地失败**。原因是 **FreeRTOS 的 PendSV 每次任务切换都会重新使能
MPU**（清 ENABLE、装载新任务区域、再置回）。保留中断恰恰让切换得以发生，把保护
在写操作底下重新打开了。

> 我当时的推理是"模块任务优先级更低，窗口期间不会被调度"——这句本身没错，但
> **不相干**：威胁不是模块任务抢占，是**任何一次上下文切换**都会重置 MPU。

最终形态：窗口缩到**单次 flash 操作**，内含 `taskENTER_CRITICAL()`。它屏蔽到优先级
5、**包含 PendSV**，所以窗口内不可能发生切换；而 USB 中断是优先级 0，高于屏蔽阈值，
**照常运行**，连 30ms 的扇区擦除都扛得住（它不调用任何 FreeRTOS API，查过）。

两次根因都是 `.noinit` fault 记录直接给出的（`mmfar = 0x0803C010` + DACCVIOL），
一眼定位，没有猜测环节。

**工具**：`persist` / `forget` 控制台命令；`CMD_LOAD_PERSIST`(4) 协议命令；
`console.py /save`、`watch.py --persist`、MCP `deploy_feature(persist=true)`。
默认都不落盘——开发循环每次存盘都擦一次扇区不合适。

### 2026-09-08 claude/opus-5（MCP A1–A3：P2 收尾）

`mdl/tools/mcp_server/`，stdio + JSON-RPC 2.0，实现 §4 的六个 tool。真机端到端
验证：整个会话 10 秒。

```
deploy self_clash → isError: USART2 和 BTN0 都是 PA3   （构建期拒绝）
device_status     → RUNNING                            （设备没被碰过 ✓）
deploy sw4_green  → OK, unloaded_previous=True
read_logs         → [host] gpio released -> default: 2
                    [mdl] sw4_green resident: ...
remove_feature    → OK
```

**"构建失败不碰设备"这条契约是验证过的**，不是写在文档里的意图。

**没有引入 `mcp` SDK**：MCP over stdio 就是换行分隔的 JSON-RPC，约百行；本项目
只有 pyserial 一个运行时依赖（`packer.py` 零依赖）。为省这一百行引入一个 SDK，
代价大于收益。

**帧格式抽成 `mdl/tools/mdl_proto.py`**：它本来要出现第三份拷贝了
（watch.py / console.py / MCP）。这和 `board_pins.def` 是同一个道理——三份手工
维护的线格式不会保持一致，而漂移的表现是"设备无响应"，正是之前查过一整轮的症状。

**`compile_check` 两档都做**（用户要求）：默认**离线**，只用源码 + `board_pins.def`，
无需串口，CI 里能跑；`against_device: true` 时额外核对设备当前 ABI 与槽位占用。
不合并成一个，否则会得到一个"没有设备就不能编译"的工具，那会毁掉 B2 的价值。

**路上抓到两个真 bug**

1. **`mock_host` 会永久挂住常驻 MDL**（早已存在，只是没人踩）。它原生执行
   `module_init()`，而 `sw3_blue` 这类模块的 `module_init` **按设计永不返回**。
   **`watch.py` 也调 mock_host，所以 `watch.py sw3_blue` 一样挂死**——项目自己的
   开发内循环。修在源头（`mock_host/run.py` 加超时），超时报告为"常驻模块，无法
   模拟"而非失败：**一个把 `module_init()` 跑到返回的模拟器，本来就模拟不了一个
   不打算返回的 `module_init()`**。
2. **每次设备请求都等满超时**。`_pump()` 收到帧仍不返回，6 秒超时 = 每次请求固定
   6 秒，一次 deploy 三十几秒。超时该用来**界定失败**，不该**给成功定价**。

**P2 至此全部完成**（B2 / P5 / A1–A3）。剩下 P3（F2 持久化、F3 原子热替换、
H10 QEMU 复跑）以及主线 agent 开的 X1/X2（RV32 PIC POC、ESP32-C3）。

### 2026-09-08 claude/opus-5（.gitattributes + B2 + P5 首批数字）

**`.gitattributes`** — 行尾由仓库决定，不再取决于各人的 `core.autocrlf`。
加完执行 `git add --renormalize .`：**没有任何文件需要改动**，因为
`autocrlf=true` 一直在提交时规范化为 LF。所以这不是修复不一致，而是把
"碰巧一致"变成"按规则一致"，顺带让这个问题不再周期性地冒出来。

**B2：冲突在构建期就拦**（P2 第一项，已验证）

设备本来就会拒，现在打包器也拒。同一句话**几分钟前**就说了，而不是等镜像推到
设备上——那时已经有人在等结果了。

```
self_clash    ✗ USART2 (PA2 TX, PA3 RX) and BTN0 (PA3, SW3) are both PA3
uart1_clash   ✗ USART1 (PA9 TX, PA10 RX) needs PA9, held by the DAP debug UART
```

> **关键决定：一份表、两个读者。** `mdl/host/board_pins.def` 存板级引脚事实，
> `host_resources.c` 用 X-macro `#include` 它，`packer.py` 用正则解析它。
> 显而易见的替代方案（把表抄进 Python）失败得很难看：两份会漂移，然后
> **构建通过、设备拒绝，两边都对自己那份笃信不疑**。解析器读到 0 行会直接
> 报错——静默解析失败会把冲突检查变成一个"永远报成功"的空操作，比没有检查更糟。

> 新增 `--allow-conflicts`：**构建期闸门一旦生效，就再也造不出故意冲突的镜像**，
> 而设备端那道检查是防"不经我们打包器的镜像"的最后防线，会因此悄悄失去覆盖。
> 三个负面 MDL 用它打包。这道闸门当场抓出第四个我没想到的：原来的 `conflict`
> 认领 GPIO(4)——**PA9 的另一个名字**。

**P5 首批数字（ABI v6）**

新增 `host->cycles()`（DWT CYCCNT）和每个事件在 ISR 里盖的周期戳。用 DWT 而不是
RTOS tick，因为 1ms 的 tick 会把这三个数**全部四舍五入成零**；`TRCENA` 由软件自
己置，不依赖调试器——只在调试器下才跑得出的基准，测的是调试器。

真机（288MHz，写缓冲已按 R2 关闭，1000 次迭代取平均）：

| 测项 | 结果 | 含义 |
|---|---|---|
| 空循环迭代 | 10.1–10.5 周期 | 基线 |
| 纯计算迭代 | 12.1–12.6 周期 | **原生 ARM 指令，无解释层** |
| **一次门控 host 调用** | **213.57 周期（净约 203）≈ 0.7µs** | 不被信任的税 |
| **中断 → handler 派发** | **n=308：min 501 / avg 1332 / max 1685 周期**<br>= **1.7 / 4.6 / 5.9 µs** | **沙箱隔离的代价** |

> 派发延迟是三个里最重要的，因为它是唯一**不放弃隔离就消不掉**的开销。
> min/max 只差 3.5 倍、上界 5.9µs 很紧——抖动来自"等更高优先级任务让出"，
> 而不是某种不确定的机制。
>
> 这把 §11.1 里"10µs 以内的硬截止期 MDL 永远做不到"从推断变成了实测：
> **上界 5.9µs 加上 handler 自身执行时间，10µs 确实是现实的分界线。**
>
> 对照 LuaTOS：它的回调要经过消息队列 → coroutine 恢复 → 字节码解释 →
> （可能）GC 停顿。**这 4.6µs 里没有任何一项可以被 GC 打断**——这就是
> "架构上做不到"的具体含义，现在有数字了。
>
> 采样期间 `329 delivered, 40 merged, 0 LOST`，合并路径同时被覆盖。

**测量方法上翻的一次车**（值得记）：第一版把延迟**在事件发生的瞬间打印**，
结果 32 个事件的日志在监听开始前就发完了、设备端不缓存、全丢。这不是运气问题
是设计错误——共享控制台上，**必须在正确时刻盯着才看得到的基准不是测量，是巧合**。
改为累积 min/avg/max + `bench` 命令按需查询。修完**不用重烧固件**，重打包推上去
即可，这本身又是一次核心主张的演示。

> **203 周期是"不被信任"的税**：每次 vtable 调用都是一趟
> `xPortRaisePrivilege`/`vPortResetPrivilege` 的 SVC 往返。对照解释器动辄数千
> 周期的函数调用，这仍然快一个量级以上；但它**不是零**，把 host 调用写进热
> 循环会付出代价。`host_api.h` 里已就此写明。
>
> 纯计算那两个数（10 vs 12 周期/迭代）证明的是另一件事：MDL 里的循环就是原生
> ARM 指令，没有解释层。这个数难看的话，项目前提就不成立。

**待办**：MCP A1–A3 尚未开始（P2 最后一项）。

### 2026-09-07 claude/opus-5（晚：R1/R2/H2/F1/R3 全部完成并真机验证）

按 §8.1 的顺序做下来，P0 和 P1 现在是空的。每一项都在真机上验过，不是编译通过就算。

**R1 卸载归还引脚** — `reclaim_module()` 从不恢复引脚配置，MDL 留下什么状态就传给
下一个。抽出 `gpio_set_default()` 作为**唯一一份**默认状态定义（开机与归还共用，
不可能漂移），先恢复后清位图。

> 测试设计上翻过一次车，值得记下来：第一版测试是"点亮绿灯 → 卸载 → 看灯灭没灭"。
> 但这块板子两个 LED 同时是 host 指示灯，卸载后 host 立刻把绿灯按 USB 状态点回去，
> 于是"MDL 残留"和"host 正常点亮"电平完全一致——**这个测试无论修没修都会报
> RESIDUE**。一个恒定给出同样结论的测试等于没有测试。改为让归还动作自己报告
> （`[host] gpio released -> default: 0 2`），报出的引脚与声明精确一致。

**R2 写缓冲** — `ACTLR.DISDEFWBUF` 默认关掉。它在 bring-up 时很值（H8/H12 都靠它），
但它让全系统每条 store 变慢，而"几乎无性能损耗"是本项目的核心主张——**留着它测出
来的数字会朝对我们有利的方向错**，那是基准测试最不该错的方向。`make PRECISE_FAULTS=1`
可开回，`ver` 报告当前镜像是哪种，避免在慢镜像上取数。

**H2 看门狗语义（ABI v3）** — 原来"调过任何 host API"就算存活，而
`while(1){ uptime_ms(); }` 能永远喂饱它，看门狗对它唯一该抓的东西是瞎的。改为只有
两件事算数：显式 `watchdog_feed()`，和从 `delay_ms()` 返回（阻塞在 host 里按定义
不是卡死）。顺带修掉一个**此前就存在**的 bug：旧 `delay_ms` 在进入等待**之前**喂狗，
所以 `delay_ms(5000)` 会在 3 秒被杀——睡觉被当成了卡死，一直没暴露只因为至今每个
MDL 都是小步长睡眠。真机双向验证：长睡活、死循环仍被杀。

**F1 中断/事件（ABI v4）** — host ISR（特权、极短）捕获边沿 → 唤醒模块任务 →
`module_event()` 在非特权任务上下文执行。ISR 里直接调 MDL 就是特权执行，沙箱作废，
这条没得选；代价是延迟为"一次任务唤醒 + 上下文切换"，比这更紧的截止期 MDL 永远做
不到，只能是 host 代码。

> **生命周期被迫统一**：控制台命令原本靠"重启模块任务"实现，而阻塞在事件等待里的
> 任务没有第二个可以重启。改成**单任务单队列**，硬件事件和控制台命令走同一条路。
> 不这么做，"能收中断"和"能答命令"会互斥，而且要等两个功能都用上才暴露。

> **溢出策略**（用户拍板：合并 + 报错 + 尽量在导入前拦）。同源事件合并而非丢弃，
> **合并计数随事件带出**——合并对状态型（引脚电平）透明，对计数型（编码器、脉冲）
> 不带计数就是静默数据损坏。真丢失（队列被别的源占满）单独计数。
> 关于"编译期拦截"：**只有一半能做**。深度是静态数字，打包器直接拒绝超预算；
> 速率是物理世界的属性，`.mdl` 里没有信息能推出来，所以记为**契约**、运行时报
> `declared 2/s, peak seen 13/s <- OVER DECLARED RATE`。把不能证明的事情说成能证明，
> 做出来的会是一个名不副实的检查。

> 真机数据：`sw3_irq` 28 delivered / 0 merged / 0 lost，且 MDL 自己的边沿计数经 `mem`
> 读回是 29（=1+28），两个独立计数器吻合。`sw3_slow`（每事件故意睡 400ms）触发了快
> 版本永远到不了的合并路径：58 边沿、31 合并、0 丢失、最终电平正确。
> 由此得到两条设计结论：**合并限制队列长度而非延迟**；**单一事件源永远不会丢**
> （同源互相合并只占一格，丢失需要多源竞争）。

**R3 资源类型扩展（ABI v5）** — I2C/UART/TIMER 资源类型，冲突判定移到**物理引脚
空间**。比名字是没用的：`MDL_RES_UART(2)` 和 `MDL_RES_GPIO(2)` 种类、编号、含义全不
同，在这块板子上却是同一根铜线（PA3）。真机三种结局全验：USART1 撞 DAP 串口被拒
（host 硬持有）、GPIO(2)+USART2 自冲突被拒（**纯凭镜像即可判定，应由打包器拦——B2**）、
I2C1+TMR3+LEDG 通过。

> 引脚表只覆盖 I2C1/USART1/USART2/TMR3，按"先跑通机制"。表里标注了一处**未经例程
> 验证**的（TMR3 CH3/CH4 在 PB0/PB1 来自复用功能表）——写错会产生**自信但错误**的
> 冲突报告，比漏报更糟。
>
> **注意范围**：R3 完成的是**仲裁**，不是**驱动**。MDL 现在能声明 I2C1 并被正确
> 拦冲突，但 vtable 里没有 I2C 操作，它驱动不了。先做仲裁是有意的——所有权搞错是
> 昂贵的错误，且必须在多个外设同时上场之前就对。

**其他** — 新增 `ver`（固件构建时间 + ABI 版本 + 写缓冲状态），起因是"板子上跑的是
不是我刚构建的那版"反复靠推断回答：**Ozone 在 `OnProjectLoad()` 里只读一次 ELF**，
之后下载的是内存里那份，重建在调试器侧是隐形的。`console.py` 加 `/load <路径>`，
在同一个已打开端口上导入 MDL（一个串口只能开一次，此前必须退出终端再开）。
清掉两轮重构留下的三处死代码。

**行尾**：仓库 `core.autocrlf=true`，提交时本就规范化为 LF，所以主线 agent 那次
"统一为 LF"对仓库内容零影响——**不存在可违反的 CRLF 约定**。真正的解法是加
`.gitattributes` 把结果钉死，与各人的 git 配置无关。未加，等用户决定。

### 2026-09-07 主线 agent（差距复核 + 可移植性分析）

- 应用户要求完成两项分析并写入新章节 **§11**：
  - §11.1 与 LuatOS 的差距复核：目标场景（热替换/隔离/确定性）上差距只剩
    F1/H2/R3 三件事；生态广度明确不追。
  - §11.2 ESP32/RISC-V 可移植性：核心风险是 **RISC-V 无 `-msingle-pic-base`**
    （需 `gp` 基址 + `-mno-relax` POC）与 ESP-IDF FreeRTOS 分支集成；
    建议路径为裸 RV32 POC → ESP32-C3 → 放弃 Xtensa。
- 任务看板新增 **X1/X2**（RV32 PIC POC、ESP32-C3 集成评估）。
- 目录补 §11 条目。**注意**：本轮因 Edit 工具对 CRLF 混排的处理问题，
  顺手将全文行尾统一为 LF（纯 LF，无混排）——内容无改动，仅行尾规范化。

### 2026-09-07 claude/opus-5（下午：三个 MDL 真机验证 + 路线图）

**三个 MDL 真机验证通过，不重烧不复位切换**

| MDL | 行为 | 结果 |
|---|---|---|
| `sw3_blue` | 按住 SW3 → 蓝灯亮 | 通过 |
| `sw4_green` | 按住 SW4 → 绿灯亮 | 通过 |
| `sw3_blink` | 按住 **同一颗 SW3** → 绿灯闪 | 通过 |

第一个和第三个是全部意义所在：**同一颗物理按键做完全不同的事，差别只是
296 字节走了一次串口**。用户实测蓝灯 1Hz 存活闪烁频率**肉眼无变化**——模块任务
是最低优先级、只在 host 任务空隙里跑，没有挤动 `indicator_task` 的节拍。这是
"几乎无性能损耗"第一次以可观察的方式出现（还不是测量，见 P5/R2）。

资源仲裁也在真机上验了两个方向：`conflict.mdl` 认领 PA9 被拒且 `status` 仍是
`EMPTY`（一个字节都没进 arena）；`sw3_blue` 加载后蓝灯引脚连采 14 次全灭
（host 停止驱动），声明转移到别的 MDL 后闪烁自动恢复。

**本轮抓到的 bug**（全部已修）
- **回归（我引入的）**：把"必须声明才能碰引脚"的检查加在了 `host_gpio_set_impl()`，
  而 `host_gpio_direct_set()` 也走它 → 没加载 MDL 时 **host 拒绝了自己使用自己的
  指示灯**，两个灯全哑。检查放错了层：它约束 MDL，不是引脚的属性。
- `reclaim_module()` 不清 `gpio_claimed` → unload 后 host 永远不收回让出的引脚。
  其余声明字段之所以没出事，只因为 `help`/`pins` 恰好都先判了 `state != EMPTY`——
  **这不是可依赖的性质，是下一个 bug 在等人来写**。全部清干净，并给
  `host_gpio_yielded_to_module()` 补了状态判断。
- `pins` 所有权列把 host 项排在模块声明之前 → 报告 host 拥有一个它已让出的引脚。
  **和硬件矛盾的所有权列比没有这一列更糟。**

**`ver`：让设备自己回答"跑的是不是我刚构建的那版"**

`board_buildid.c` 每次 make 强制重编（`FORCE` 依赖），所以 `__DATE__ __TIME__` 是
镜像的构建时刻。起因是这个问题反复靠推断回答：**Ozone 在 `OnProjectLoad()` 里只读
一次 ELF，之后下载的是内存里那份**，重建在调试器侧是隐形的，一整轮测试可能花在上一
个镜像上。真机核对通过。

**术语定案：这些单元叫 MDL**

不是"固件"（那是烧进去、独占机器、要重启的东西），也不叫"模块"（嵌入式语境里已被
硬件模组占用）。`.mdl` 后缀、`MDLC` 帧魔数、`mdl_load()`、`mdl>` 提示符本来就全是
这个词，零成本。ABI 符号 `module_init`/`module_cmd` 不动——那是入口函数名，不是名词。

**工具**
- `console.py` 加本地命令：`/load <相对或绝对路径>` 在**同一个已打开的端口**上推 MDL，
  并解码返回的二进制帧。此前导入必须退出终端 → 跑 watch.py → 再开回来，就因为
  一个串口只能被打开一次。
- `watch.py` 接受直接推成品 `.mdl`：拿三个做好的 MDL 试用和开发一个 MDL 是两回事，
  前者不该要求装 ARM 工具链。

**代码已推送** `git@github.com:overrize/embedded_sandbox.git` `main` 分支。
本机原先没有任何 SSH 密钥，新生成 ed25519 一把。

**优先级路线图见 §8.1**，对标 LuaTOS 的逐项定位见 §8.2。一句话：
中断（F1）是**补课**不是差异化；资源仲裁和确定性才是护城河；生态广度短期追不上，
也不该当目标。

### 2026-09-07 claude/opus-5

**MDLC 帧路径：已验证可用**
- `CMD_STATUS`(13B) 真机回了合法 `RESP_STATUS` 帧；坏 magic 回
  `bad magic (not an MDL0 image)`，坏 CRC 回 `crc32 mismatch (corrupt transfer?)`。
  → magic 增量匹配、与控制台文本的分用、CRC、帧组装、supervisor 唤醒、
  `mdl_load()` 的全部校验，**这些都是好的**。H7 的"MDLC 帧共用尚未实测"可以销掉。
- 主机端 CRC 与设备端 `mdl_crc32_2()` 数值核对一致（`0xECB3F528`），
  两边都是标准 CRC-32（反射，`0xEDB88320`）。

**H12：模块一加载就炸，已二分到最小复现**
- 现象：写下 `CMD_LOAD` 帧后 USB 从总线掉线（`ClearCommError` /
  `PermissionError(13)`），随后设备重新枚举，`status` 显示 `slot: EMPTY`、
  `fault: none on record`——**证据随复位蒸发**。
- 三个模块依次推送，全炸：

  | 模块 | text | got | relocs | 内容 | 结果 |
  |---|---|---|---|---|---|
  | minimal | 8B | 3 | 0 | `return 7;` | 炸 |
  | nohost | 24B | 4 | 1 | 一个全局变量 | 炸 |
  | hello | 180B | 7 | 4 | 全局 + `host->log()` | 炸 |

  8 字节的 `movs r0,#7; bx lr` 都炸，**排除**重定位、GOT、host vtable、
  payload 大小、突发/流控（32B/60ms 限速同样炸）。
- 也逐一排除了：`init_off=0x1`（Thumb 位正确，非 INVSTATE）；
  arena 由 `mdl_arena.ld` 以 `(NOLOAD) ALIGN(65536)` 真实预留，不与 `.bss`/堆重叠；
  模块栈 `0x20027800` 对 2KB 自对齐，`xRegions[0..2]` 基址/长度均合法；
  四个 fault handler 都已链进 ELF。
- 剩下的嫌疑范围：`xTaskCreateRestricted` 到首次切入非特权模块任务这一段。
  这正是 M2 建立、但**至今在任何环境（含 QEMU）都没真正跑通过**的那条路，
  与 H10 记的 QEMU PendSV 阻塞很可能同因。

**里程碑达成：完整模块（含 host API 调用）真机跑通**

`hello.mdl`（288B，2 个全局、4 条重定位、3 次 `host->log()`、1 次 `host->gpio_get()`）：

```
-> RESP_OK
[module] hello module: module_init running     ← 模块日志从 USB 控制台出来
[module] hello module: counter ok
status : slot : LOADED (ran, idle)   fault : none on record
mem 0x20024000 12:
  0x20024000: 0x00001218 0x00000000 0x00000000 0x20020054
  0x20024010: 0x20020070 0x2002008C 0x2002401C 0x0000002A
                ↑ text-base 重定位（字符串字面量）  ↑ data-base   ↑ g_counter = 42
```

GOT 里三个槽指回模块 text（`0x2002xxxx`，字符串字面量），一个槽指向模块 data
（`0x2002401C` = `&g_counter`），而那个地址上就是 `0x2A`。`1 + 41 = 42` 是模块
自己算的、通过重定位后的指针写进去的。

至此 §1 的目标在真机上端到端成立：**不烧录、不重启，串口送进去的代码改变了 MCU 的行为。**

**里程碑：模块首次在真机上跑通（不重新烧录）**

推 `nohost.mdl`（96B，一个全局变量 + 一条 `R_ARM_RELATIVE`，不调 host API）：

```
-> RESP_OK
status : slot : LOADED (ran, idle)   entry : 0x20020001
mem 0x20024000 8:
  0x20024000: 0x00001164 0x00000000 0x00000000 0x20024010
  0x20024010: 0x0000002A                        ↑ GOT 槽已重定位到运行时数据基址
              = 42 = g_v 的 1+41
```

代码经 USB 送入 → 装进链接脚本预留的 SRAM arena → GOT 重定位 → MPU 下非特权执行
→ 通过重定位后的指针改写自身全局 → 干净返回。**§1 的核心主张到此为止是被证明的。**

**H15：`host->log()` 用半主机，真机上必炸（已修）**

`hello`（带 `host->log()`）仍然炸，但故障签名与 H12 完全不同：

```
hfsr  : 0x80000000   DEBUGEVT（不是 FORCED）
cfsr  : 0x00000000   一个 fault 状态位都没有
pc    : 0x08012D36   semihost_write0, host_api.c:168
lr    : 0x0801019F   host_log_impl, host_api.c:191
```

CFSR 全零 + HFSR.DEBUGEVT = 执行到了 `bkpt`，不是内存违例。`host_log_impl()` 走的是
ARM 半主机 `SYS_WRITE0`（`bkpt 0xAB`）。`bkpt` 在 `DHCSR.C_DEBUGEN == 0` 时**不是
静默失败**，它触发调试事件、在没有 debug monitor 的情况下直接升级成 HardFault。
所以任何调用 `log()` 的模块在脱机板子上都会打死设备。

源码里那句 `/* placeholder until M4's USB CDC */` 说明这本来就是临时方案，
只是 M4 的控制台做好之后没换掉。改为弱符号 sink `host_log_sink()`：
默认实现只在真的挂了调试器时才发半主机，`mdl/transport/console.c` 提供强实现
把模块日志打到 USB 控制台（前缀 `[module] `）。

**H12 根因（已修）：MPU 区域属性用错了编码体系**

`mdl_start_module_task()` 传的是 `task.h` 的抽象标志：

```c
tskMPU_REGION_READ_ONLY     = (1UL << 0)   /* 0x01 */
tskMPU_REGION_READ_WRITE    = (1UL << 1)   /* 0x02 */
tskMPU_REGION_EXECUTE_NEVER = (1UL << 2)   /* 0x04 */
```

这套标志是给 **ARMv8-M** 端口用的，那边 `vPortStoreTaskMPUSettings()` 会翻译。
**ARMv7-M 端口不翻译**，直接把 `ulParameters` OR 进 RASR，而 RASR 的位是
bit0=ENABLE、bits[5:1]=SIZE、bits[26:24]=AP。后果有两重：

1. **AP = 000 = 任何权限都不可访问**。不是"非特权被拒"——特权 handler 模式
   一样被拒。这就是为什么故障出现在 PendSV 里而不是模块代码里。
2. **SIZE 被多 OR 了 `0b011`**。8K 的 data 区编码 `0x18`，OR `0x06` 得 `0x1E`
   → SIZE=15 → 区域变成 **64K**；MPU 会忽略低于区域大小的基址位，于是它实际
   覆盖 `0x20020000..0x2002FFFF`，**把模块任务自己的栈吞了**。它是 region 6，
   栈是 region 4，重叠时高编号胜出，所以正确设好的栈区被它压掉。

净效果：加载**任何**模块（8 字节的 `return 7;` 也算），首次上下文切换进模块
任务时 `PendSV_Handler` 的 `ldmia r0!` 就吃 MemManage DACCVIOL。

真机取到的证据（`.noinit` 记录跨复位存活）：

```
which : MemManage in HOST code (pc outside module text)
pc    : 0x0800BEEC   = PendSV_Handler, port.c:549, ldmia.w r0!,{r3..lr}
r0    : 0x20027FB0   = 模块任务栈指针（在 0x20027800..0x20028000 内）
xpsr  : 0x2101000E   IPSR=14 = PendSV
cfsr  : 0x00000082   DACCVIOL + MMARVALID
MPU_CTRL = 0x00000005  PRIVDEFENA=1（所以背景区本该放行特权访问）
```

修复：改用端口自己的原始 RASR 位 `portMPU_REGION_READ_ONLY` /
`portMPU_REGION_READ_WRITE | portMPU_REGION_EXECUTE_NEVER`，并补上
`configTEX_S_C_B_SRAM`（端口只给它自建的栈区加，可配置区域不加；缺了就是
Strongly-Ordered 内存，非对齐访问会 fault）。

> 这条**极可能就是 H10 记的 QEMU PendSV 阻塞的同一个根因** —— 同一份端口、
> 同一段 `mdl_start_module_task()`、同样发生在"第二次任务切换"。

**仪器自身的两个缺陷（同时修掉）**
- `fault_arm.c` 在 `SCB->CFSR = cfsr`（write-1-to-clear）之后才让我读 MMFAR，
  那时它架构上已是 UNKNOWN。第一次真机读数里 `mmfar = 0xE000ED34` 就是这么
  来的假值——差点把我引去查 SCB。改为把清除前捕获的 `mmfar` 作为参数传入。
- MemManage 路径拿不到 EXC_RETURN，却仍按全 1 去解"来自 PSP 还是 MSP"，
  必然报错。改为未捕获时明说未捕获，并改从 xPSR 的 IPSR 报告异常号。

**H13：让 fault 活过复位（没有这个就没法继续查 H12）**
- 旧的 `board_fault_record()` 结尾是 `bkpt #0` + 死循环。没接调试器时
  `bkpt` 不是被忽略，而是在 fault handler 内再次 fault → **LOCKUP**：
  CPU 停摆、USB 不再应答、记录不可达。任何模块 fault 都长成
  "设备凭空消失"，这是 H12 一直查不动的直接原因。
- 改为：`.noinit` 段承载 `g_board_fault`（链接脚本新增，落在
  `_ebss` 之上；启动代码 `bcc` 是左闭右开，确认不会被清零），
  记录写完 `__DSB()` 后——有调试器则 `bkpt`，否则 `NVIC_SystemReset()`。
- 新增控制台 `fault` 命令，打印 pc/lr/xpsr/cfsr/hfsr/bfar/mmfar/excret，
  并把 IACCVIOL / DACCVIOL / MUNSTKERR / MSTKERR / IBUSERR / PRECISERR /
  IMPRECISERR / UNDEFINSTR / INVSTATE / INVPC / UNALIGNED 逐位译成人话，
  外加"来自 PSP 还是 MSP"。`board_debug_print_fault()` 收一个输出函数指针
  而不是直接调 `mdl_console_puts`——M0/M1 链接 `board_debug.c` 但没有控制台。

**H14：构建脚本在说谎**
- `build.ps1` 用 `Test-Path build/firmware.elf` 判定成功。编译报了 6 条
  `error:` 之后，因为上一轮的 elf 还在，它打印了 `built, with warnings` +
  `BUILD OK`。红灯被涂成绿灯，比不检查更糟。
- 改为以 `$LASTEXITCODE` 为准，并把 error 与 warning 拆成两个桶。

**工具**
- `watch.py --verify`：`RESP_OK` 后不关端口，在**同一个端口会话**里跑
  `status` / `mem 0x20024000 12` 并打印设备原文。板子只有一根线供电，
  断开重连=掉电=模块蒸发，所以验证必须留在同一会话内。
- `console.py` 去掉了开口时的 `

` 探测字节（它和 banner 抢时序）；
  `console.c` 把 `
` / `
` / `

` 统一算一次回车（CRLF 双提示符）。

### 2026-09-06 claude/opus-5

**硬件接入**
- 定位并解决 J-Link 连不上：Ozone 接口设成了 JTAG（日志里 `TotalIRLen = ?` /
  `IRPrint = 0x..0000` 是 JTAG IR 扫描的特征，板上只引出 SWD），且
  **`DAP_CONF` 未接地**导致板载 CMSIS-DAP 抢总线。
- 从原理图核对：USB1 走 PA11/PA12（OTGFS1，与 `usb_conf.h` 一致），
  **device-only 非 OTG**（VBUS/CC 都没接 MCU，且模板分配的 VBUS/ID 引脚
  PA9/PA10 被 DAP 的 UART 桥占用——绝不能给它们配 MUX）。
- Ozone 工程合并为唯一一个 `mdl/tests/hil/ozone/mdl.jdebug`。

**修复**
- H1 板级配置：`-DAT32F435VCT7`、Flash 256K、链接脚本重命名 xG→xC、
  `HEXT_VALUE` 8MHz→24MHz。
- H4 构建腐坏：M0/M1/M3 修好（弱符号 `mdl_supervisor_wake_from_isr` /
  `mdl_transport_write`，新增裸机 `at32f435_m1/host_api_m1.c`），
  新增 `build.ps1` 一键五目标。
- H3 `MEMFAULTENA`；H6 非特权 trampoline 提权。
- GPIO 白名单从 PA0/PA1 占位换成真实引脚（PD10/PE15 LED、PA3/PE2 按键），
  并补上从来没做过的引脚初始化（`host_api_init()` 原本是空函数）。

**新增**
- `common/board_clock.c`：**24MHz 下不能照抄厂商 PLL 参数**——厂商的
  `(ns=144, ms=1, fr=4)` 标注前提是 8MHz，24MHz 下算出 864MHz，且 `ms=1`
  时 PLL 参考输入 24MHz 已超出驱动文档 `2..16MHz` 上限。改用 **`ms=3`**：
  24/3=8MHz 参考 → VCO 1152MHz → 288MHz，落在与厂商完全相同的工作点。
  USB 48MHz 由 288/6 分频（原 HICK+ACC 无晶振方案作废，板上有真晶振）。
- `transport/console.{c,h}`：文本控制台与 MDLC 二进制帧**共用同一个 CDC 口**，
  靠逐字节匹配魔数前缀解复用（不用 4 字节滑窗，否则打字滞后 3 个字符）。
  命令在 supervisor 任务里执行，与故障恢复/看门狗单线程串起来。
- `common/board_debug.c`：精确故障捕获（H8 用）。

**H8：真机第一个 bug，已定位并修复**
- 症状先是 imprecise BusFault 升级成 HardFault，PC 报在 `vListInsertEnd` —— 是噪声。
  加 `ACTLR.DISDEFWBUF` 后变精确，定位到 `str r1,[r2,#4]`，即
  `pxIndex->pxPrevious->pxNext = ...` 写向非法地址。
- 根因：链接脚本把 32KB `.privileged_data` 留在启动拷贝/清零范围之外，
  FreeRTOS 全部 `PRIVILEGED_DATA` 上电即随机值。详见 §3。
- 已修 + 重编五目标 + 核对 map 里 LMA 连续性。**尚未复测真机。**

**H11：真机第二个 bug（H8 修好后浮出来的），已修**
- BusFault 消失，调度器启动，随即 MemManage `CFSR=0x00000008`（MUNSTKERR）。
- 根因：**全仓库从未使用 `portPRIVILEGE_BIT`**，所有宿主任务都是非特权的，
  而它们的栈来自 `PRIVILEGED_DATA` 里的 FreeRTOS 堆 → 异常返回出栈即被拒。
- M2/M3/M4/QEMU 四处全部补上；模块任务保持非特权不动。

**✅ 真机跑通了（2026-09-06）—— 项目第一次在真硅片上端到端工作**

重烧后 USB 正常枚举为 `USB\VID_2E3C&PID_5740` → **COM5**，控制台实测：

```
>>> clk
system_core_clock = 288000000 Hz
  (PLL from 24MHz HEXT: 24/3*144/4 -- at spec maximum)
>>> btn 2
1  (idle)
>>> status
slot : EMPTY / fault : none on record
```

同时证明：24MHz 晶振起振 + `ms=3` 的 PLL 锁到 288MHz、USB 48MHz 分频正确、
FreeRTOS-MPU 调度器稳定、CDC 收发、控制台回显与命令分发、真实 GPIO 读写。
**H12 之前怀疑的 0Ω 跳线是虚惊——板上焊的就是 R69/R70（PA11/PA12）。**

**H9（技术债 #7）：跑通当场就被 `arena` 命令抓出来了**
- `arena` 打印全零 → M4 从不调用 `sandbox_init()`，`g_mdl_slot` 的边界是 0，
  这时推一个 `.mdl` 进去会 memcpy 到地址 0。
- 拆出 `sandbox_bounds_init()`（只记边界、不碰 MPU），M4 调它；
  M2+ 的 MPU 区域本来就归 FreeRTOS 按任务管，`sandbox_init()` 写的
  region 4..7 在模块跑起来前必被覆盖，那半边对 RTOS 目标没有意义。
- M3 原先在 `supervisor_task` 里内联手写这段赋值，**而且漏了
  `guard_lo/guard_hi`**（`mdl_record_fault()` 的分类要用），一并换成调用。
- M3/M4 的 Makefile 之前都没有编 `sandbox.c`，补上。

**H12：USB 排查（已闭环，保留作参考）**
- H8+H11 修完后固件不再崩，但主机侧**连"未知设备"都看不到**
  （只有 J-Link 在线；`VID_2E3C/PID_5740` 全无）。
- 已核实软件侧完好：`OTGFS1_IRQHandler` 是自己的实现（非 `Default_Handler`）、
  `usb_cdc_init` 在 `main` 里被调用、`usbd_init`→`usbd_connect`→`usb_connect`
  链路完整（驱动内部就会释放 D+ soft-disconnect）。
- 为定位加了三样：
  1. `main.c` 的 `heartbeat_task`——1Hz 闪 LEDB，把"固件没跑"和"USB 没通"分开；
  2. `board_clock.c` 显式 `crm_usb_clock_source_select(PLL)`（原先靠
     `crm_reset()` 清 `misc1` 的副作用生效，能用但脆）；
  3. `usb_conf.h` 补上 **OTGFS2 分支**——原先只有 `#if (OTG_USB_ID == 1)`，
     改成 2 会编不出来。现在切到 PB14/PB15 就是改一个数字。
- 头号嫌疑仍是板上 0Ω 跳线选的是 R71/R73（PB14/PB15）而非 R69/R70（PA11/PA12），
  待万用表确认。

**未完成 / 交接**
- 复测 M4（H8 + H11 + H12）；按 H10 复跑 QEMU。
- **本次所有改动仍只做到编译与 map 验证，没有一行在真机上跑通过。**

### 2025-XX-XX 主线 agent

- 完成 `mdl/` 全量代码走读；确认 D1 单槽互斥语义与现状代码一致。
- 与用户对齐：资源竞争在编译期隔离（D2）；MCP 接口出草案（§4）。
- 建立本协作文档；待办：冻结 §4 → 认领 A1。

<!-- 同事从这里往上追加 -->

---

## 10. 同事工作区

> 本节预留给同事 agent：自由记录调研笔记、方案草稿、疑问。
> 建议每人一个四级标题，如 `#### 张三 / agent-2`，他人勿改动不属于自己的段落。

#### （示例）张三 / agent-2

- 疑问：watchdog_feed 进 ABI v2 后，旧模块（v1）在新固件上还能跑吗？
  → 能：append-only，v1 模块不调新函数即可；packer 按 abi_ver 精确匹配，
  固件升 v2 后旧 v1 包会被拒——这里要不要允许 "v1 模块跑在 v2 固件"？需讨论。

<!-- 同事工作区 -->

---

## 11. LuatOS 差距复核 + ESP32/RISC-V 可移植性（2026-09-07 主线 agent）

> 本节是"目标场景对标"与"跨架构迁移"两问的当前结论。
> §8.2 是 2026-09-07 的定位表，本节在其基础上按真机验证后的现状复核。

### 11.1 与 LuatOS 的差距复核（真机验证后的现状）

**已经反超的（LuatOS 因架构做不到）：**

| 能力 | 现状证据 |
|---|---|
| 不烧录、不复位换功能 | 真机三 MDL 切换通过（§9 2026-09-07 下午） |
| MPU 硬件隔离 + 故障自愈 | M2/M3 设计完成；fault 记录跨复位存活（H13） |
| 资源声明 + 冲突拦截 | `conflict.mdl` 认领 PA9 被真机拒绝（B1 已验证） |
| 原生执行、无 GC 停顿、μs 级确定性 | 蓝灯 1Hz 无肉眼抖动（还不是测量，见 P5/R2） |

**仍然落后的（按追平成本排序）：**

| 差距 | 现状 | 追平路径 | 量级 |
|---|---|---|---|
| ~~中断/回调 API~~ | **已完成并真机验证**（F1 / ABI v4） | — | — |
| 外设广度 | **仲裁机制已完成**（R3：I2C/UART/TIMER 资源类型 + 引脚级冲突，真机验证）；但 **vtable 里还没有外设操作**——MDL 能声明 I2C1，不能驱动它 | 逐个加驱动 | 中，每类外设一遍 |
| ~~看门狗误杀常驻模块~~ | **已完成并真机验证**（H2 / ABI v3） | — | — |
| 持久化 | SRAM，掉电即失 | F2 | 中 |
| 原子热替换 | unload→load 有空窗 | F3 | 中 |
| 网络/协议栈 | 零 | 不在当前路线图 | 很大，建议不做（不是目标场景） |
| 开发门槛 | 需 C + ARM 交叉工具链 | MCP-A 后 agent 闭环掩盖此差距；人类用户仍比写 Lua 难 | 由工具链缓解，不做语言层 |
| 生态/多芯片 | 单芯片、零生态 | **明确放弃**，不是目标 | — |

**结论（2026-09-07 晚更新）**：写这一节时列的 F1/H2/R3 三件事**当天全部完成
并真机验证**，所以在"功能热替换 + 资源确定性"这个目标场景上，架构性差距已经
清零。剩下的是**广度**不是**能力**：外设驱动要一类一类加，而每一类都已经有现成
的仲裁机制兜着（声明 → 引脚级冲突 → 加载期拒绝 → 卸载归还）。

在"通用脚本生态"上差距巨大但**不该追**——那是 LuatOS 的护城河，我们的护城河是
它架构上做不到的事。这一点原作者说得对，不改。

### 11.2 ESP32 / RISC-V 可移植性分析

**架构上已经为移植留好的口子：**

- `core/` 只依赖 `arch_if.h` 的 5 个函数，`MDL_ARCH_RV32=2` 枚举已占位；
- `mdl_format.h` 的 region 注释已明确"ARM MPU 高编号赢 / RISC-V PMP 低编号赢"
  的不对称性收在 `arch_setup_regions()` 后面——**当初就防了这一手**；
- arena 按 2 的幂自对齐划分（`mdl_arena.ld`），天然同时满足 ARM MPU 与
  RISC-V PMP NAPOT 的要求；
- transport 介质无关，MDLC 帧协议与承载解耦（UART fallback 注释已预留）；
- packer 只认 `R_ARM_RELATIVE` 一种重定位——加 `R_RISCV_RELATIVE` 是同构扩展；
- MCP server / manifest 机制（MCP-A/B）纯 PC 侧，与目标架构无关。

**必须新做的（按风险排序）：**

| 风险点 | 说明 | 风险 |
|---|---|---|
| **RISC-V 无 `-msingle-pic-base`/`r9`** | 这是当前 PIC 机制的根基，ARM-only。RV32 替代方案：`gp`(x3) 作 GOT 基址 + `-mno-relax`（链接器松弛会绕过 GOT 破坏可重定位性），入口 glue 里设 gp。**必须先做 POC 验证 gcc/ld 对 RV32 `-fpic` 的真实输出** | 高 |
| FreeRTOS RV32 MPU(PMP) 端口 | 10.6+ 有官方 rv32i-mpu 端口；但 **ESP32-C3 走 ESP-IDF 的 FreeRTOS 分支**，与上游 MPU 端口合并不直接可用，集成是工作量大头 | 高 |
| 故障恢复 | MemManage → RISC-V trap（mcause/mepc/mtval）分类，PC-patch-to-trap-loop 要重写 | 中 |
| I-cache | ESP32-C3 有缓存，写代码进 SRAM 后必须 `icache invalidate`；`arch_code_sync` 钩子已留好，CM4 的 DSB+ISB 不够 | 低 |
| USB | ESP32-C3 有 USB Serial/JTAG（可枚举 CDC）；经典 ESP32 无原生 USB，走 UART | 低 |

**平台结论：**

| 平台 | 可行性 | 建议 |
|---|---|---|
| RISC-V 通用 MCU（CH32V307 等） | 可行，裸机/裸 FreeRTOS 集成路径清晰 | 优先做 RV32 POC，比 ESP32 先打通 |
| **ESP32-C3**（RISC-V） | 可行但最贵：IDF FreeRTOS 分支集成是主要成本 | 排在裸 RV32 之后 |
| ESP32 / ESP32-S3（Xtensa） | 不建议：windowed register ABI 让裸 asm 上下文切换极痛，Xtensa 工具链 PIC 支持弱 | 放弃或无限期搁置 |

**建议路径**：先做一个最小 RV32 POC——只验证"`-fpic`+`gp` 基址 + packer 加
`R_RISCV_RELATIVE` + PMP 四区域"三件事能否跑 `hello.mdl` 等价物，不碰 ESP-IDF。
POC 过了再谈 ESP32-C3。见任务看板 X1/X2。
